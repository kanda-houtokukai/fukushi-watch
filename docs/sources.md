# 監視対象台帳（sources.md）

福祉情報ウォッチが巡回する情報源の正本。**この表がP1以降の実装の入力になる。**

## 運用ルール

- **P0時点では「器だけ」**。URLの調査・確定は **P1（まず1源だけ）** と **P4（残り4源）** で行う。
- 1源 = 1行。行の追加・削除・状態の変更は、必ずこのファイルへ反映してから実装に渡す。
  **P4以降は、監視源の増設はこの表に1行足すだけで済む状態にする**（コード側を触らない）。
- **確定済みの行は承認なしに書き換えない**（CLAUDE.md「触ってはいけない場所」）。
- 状態の語彙: `URL未確定` → `URL確定` → `巡回方法確定` → `巡回中` → `停止中`
- **巡回方法の列には `scripts/crawl.js` のパーサ名を書く**（例: `cfa-cards`）。
  `crawl.js` は状態が「巡回中」の行だけを巡回する。新しい構造の源を足すときは
  パーサを1つ実装し、その名前をこの列に書く。

## 台帳

| # | 源の名前 | URL | 巡回方法 | 状態 |
|---|---|---|---|---|
| 1 | こども家庭庁 | https://www.cfa.go.jp/news | cfa-cards | 巡回中 |
| 2 | 厚生労働省 | https://www.mhlw.go.jp/stf/new-info/ | mhlw-news | 巡回中 |
| 3 | WAM NET | https://www.wam.go.jp/gyoseiShiryou/new_rss | wam-rss | 巡回中 |
| 4 | 福岡県（障がい福祉） | https://www.pref.fukuoka.lg.jp/life/3/27/ | fukuoka-life | 巡回中 |
| 5 | 福岡県（子ども・青少年） | https://www.pref.fukuoka.lg.jp/life/3/39/ | fukuoka-life | 巡回中 |
| 6 | 報酬改定関連 | なし | — | 他源でカバー |

> **P1で扱うのは、このうち1源だけ**（どの源を1本目にするかはP1の指示書で指定される）。
> 残り4源は **P4** で確定する。

## 各源の補足（URL確定時に埋める）

- **こども家庭庁**: ✅ P1で確定（2026-08-16）。`/news` を**全カテゴリ**で監視する [DECISION]。
  カテゴリ（報道発表・政策・法令等の10種）は機械では絞らず、項目にカテゴリ名を保存して
  P2の重要度判定に渡す（機械で絞ると例外を永久に取りこぼすため。絞りたくなったらP2側で調整）。
  RSSなし・静的HTML・robots.txt許可済み。巡回方法 `cfa-cards` は `scripts/crawl.js` のパーサ名。
- **厚生労働省**: 障害・介護分野の一次情報源。対象ページの粒度を確定する。
- **WAM NET**: 行政情報の集約先。一次情報への導線として使うか、それ自体を監視するかを判断する。
- **福岡県**: ✅ P5で確定（2026-08-16）。分野別更新一覧を**2行に分割**して監視
  （障がい福祉 `/life/3/27/`・子ども・青少年 `/life/3/39/`。同一構造なのでパーサは共通）。
  ⚠️ robots.txt が `/rss/` と `/kodomo/` を Disallow のため RSS は使わない。
  `/life/`・`/contents/` は許可済み。市町村レベルへの拡張は将来の判断。
- **報酬改定関連**: ✅ P5で判断（2026-08-16）[DECISION] **独立した源としては不採用**。
  報酬改定ナビ（houshu-navi）は静的な知識ベースであり新着の流れる場所ではない。
  報酬改定の新しい通知・Q&Aは 厚労省新着・WAM NET行政資料・こども家庭庁で完全にカバーされる
  （独立URLを持たせると二重検知になるだけ）。行は経緯の記録として残す。
