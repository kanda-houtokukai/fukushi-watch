# CLAUDE.md（福祉情報ウォッチ）

> Claude Code はセッション開始時にこのファイルを自動で読む。

## この案件について

- 案件名: 福祉情報ウォッチ（行政の福祉関連情報の監視ボット）
- 種別: **個人の試験開発**（形になったら PT・県協会への提示物に昇格する想定。
  現時点では法人PT案件ではないため、dev-workflow の `references/pt-context.md` は適用しない）
- 台帳: fukushi-watch-handoff.md

## セッション開始時に必ずやること

1. 上記の台帳（handoff）を読み、現在地・決定事項・教訓を把握する
2. 作業規範は dev-workflow スキル（~/.claude/skills/dev-workflow/）に従う
3. 把握が終わるまで、実装・編集・デプロイをしない（鉄則1）

## 台帳の維持（肥大させない）

- 台帳は**毎セッションが起動時に読む**。肥大するとコンテキストを毎回浪費する。
- **版/フェーズの記録は直近10件まで。** 11件目を足す前に、最も古い1件を
  `fukushi-watch-archive-YYYY-MM.md` へ**原文のまま**移してから追記する。
- **台帳が3.2万文字を超えたら**、まず記録の件数を数える:
  - 規定数超過 → 最も古いものをアーカイブへ移す
  - **規定数以内でも超えている場合** → **ロードマップの完了済みの行を、原文のまま
    アーカイブへ移す**。台帳側には「移設先を見よ」の一文を残し、
    **どのフェーズで何をしたかを辿る入口を必ず確保する**（2026-08-25にP0〜P20で実施）。
  - ⚠️ **決定の経緯（[DECISION]）と「生きている注意事項」は、圧縮も移設もしない。**
    前者は判断の理由が失われ、後者は毎回読まれることに意味があるため。
- ⚠️ **しきい値は「文字数」で測る。`wc -c` はバイト数を返し、日本語は1文字3バイトなので
  約2.3倍に出る。** 測り方は `python3 -c "print(len(open('FILE',encoding='utf-8').read()))"`。
  実例（2026-08-27）: wc -c で 85,942 を「字」と報告し、実際の 37,927字（26%超過）を
  「2.9倍の超過」と誤認した。この誤認は、生きている記録を機械的にアーカイブへ
  落とす事故に直結する。
- ⚠️ **移設対象は「最古の1件」ではなく「完了済みの記録のうち最古の1件」。**
  最古が進行中の記録である場合は飛ばして次を見る。進行中の記録は、
  完了するまでアーカイブへ移さない。
- ⚠️ **要約・圧縮による削減は禁止**（アーカイブは原文保存が目的）。

## このリポジトリ固有の注意

- 実行基盤: **GitHub Actions の定期実行（毎朝7時）**。導入は P3。それまでは手動実行のみ。
- 状態保存: **リポジトリ内の JSON**（DBを使わない構成。報酬改定ナビで実績あり）。
- ⚠️ **鍵類の値を、リポジトリ・台帳・チャットに絶対に書かない。**
  Gemini API キー・Gmail アプリパスワードは **GitHub Secrets にのみ**保管する。
  ドキュメントに書いてよいのは「シークレット名」まで（値は書かない）。
- ⚠️ **運用コストは完全ゼロを維持する。** Gemini API は無料枠、GitHub Actions は無料枠の範囲。
  課金が発生する選択肢は採用前に必ずユーザーに確認する。
- 触ってはいけない場所:
  - `docs/sources.md`（監視対象台帳）の**確定済みの行**は、承認なしに書き換えない。
  - **差分スナップショットに監視先ページの全文を保存しない**（ハッシュ＋見出し等の最小限。
    理由は台帳の「生きている注意事項」）。
- その他:
  - **P0ではコードを書かない**。P1の実装指示書が出るまで実装に着手しない。
  - **外部の鍵が要るフェーズ（P2・P3）は、指示書の冒頭に手順0（設定の雛形→入手先の案内→
    いったん停止して待つ）を置く**。実装を始めてから鍵が無いと気づく事故を防ぐ。
  - 設計判断を迫られたら勝手に決めず、Chat でのすり合わせに差し戻す（dev-workflow §7-4）。
