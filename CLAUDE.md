# CLAUDE.md（福祉情報ウォッチ）

> Claude Code はセッション開始時にこのファイルを自動で読む。

## この案件について

- 案件名: 福祉情報ウォッチ
- 種別: 法人PT案件（社会福祉法人豊徳会・AI業務効率化PT）
- 台帳: fukushi-watch-handoff.md

## セッション開始時に必ずやること

1. 上記の台帳（handoff）を読み、現在地・決定事項・教訓を把握する
2. 作業規範は dev-workflow スキル（~/.claude/skills/dev-workflow/）に従う
   - 法人PT案件のため `references/pt-context.md`（PT共通前提）も必ず読む
3. 把握が終わるまで、実装・編集・デプロイをしない（鉄則1）

## 台帳の維持（肥大させない）

- 台帳は**毎セッションが起動時に読む**。肥大するとコンテキストを毎回浪費する。
- **版/フェーズの記録は直近10件まで。** 11件目を足す前に、最も古い1件を
  `fukushi-watch-archive-YYYY-MM.md` へ**原文のまま**移してから追記する。
- **台帳が3.2万文字を超えたら**、まず記録の件数を数える:
  - 規定数以内 → 規則は守られている。重い回が続いただけ。何もしない
  - 規定数超過 → 最も古いものをアーカイブへ移す
- ⚠️ **要約・圧縮による削減は禁止**（アーカイブは原文保存が目的）。

## このリポジトリ固有の注意

- デプロイ方法: **P0時点では未定**。現時点の公開手段は GitHub リポジトリ
  （kanda-houtokukai/fukushi-watch）への push のみ。閲覧用の配信方法（GitHub Pages 等）は
  P1以降に決定する。決まるまで公開先を勝手に作らない。
- 触ってはいけない場所:
  - `docs/sources.md`（監視対象台帳）の**確定済みの行**は、承認なしに書き換えない。
    P0時点では全行が「URL未確定」であり、URLの調査・確定は P1 で行う。
  - **差分スナップショットに監視先ページの全文を保存しない**（詳細は台帳の「生きている注意事項」）。
- その他:
  - **P0ではコードを書かない**。P1の実装指示書が出るまで実装に着手しない。
  - 設計判断を迫られたら勝手に決めず、Chat でのすり合わせに差し戻す（dev-workflow §7-4）。
