# 福祉情報ウォッチ — 引き継ぎ台帳（handoff）

> 引き継ぎの入口はこのファイル1本。新しいチャットは、まずここを読んでから作業を始める。
> 二層運用: ①現在地サマリ（毎回読む）②経緯アーカイブ（必要なときだけ読む）。

---

# 現在地サマリ

## この案件は何か

行政（こども家庭庁・厚労省・自治体等）の福祉関連情報を**毎日巡回して更新を検知し、
要約と重要度判定を付けて毎朝メールで届ける**監視ボット。
**巡回 → 差分検知 → 要約 → 重要度判定 → 通知**の5役が連携するマルチエージェント構成。
目的は①実用ツール（毎朝メールで更新を受け取る）②マルチエージェント設計の学習、の2本立て。

## 現在地

- **P22まで完了（2026-08-25）。実運用中＋一般公開中。** 毎朝7時にGitHub Actionsが
  巡回→差分検知→判定（要約・重要度・分野タグ・対応の目安）→メール送信→履歴蓄積→
  締切抽出→状態コミットを自動実行する。
- 巡回しているのは**6口**: こども家庭庁・厚労省・WAM NET・福岡県×2（行政5）＋介護ニュースJoint（報道）。
  **全国社会福祉協議会（団体・P21で実装済み）は「保留（届出前）」で巡回対象外**——
  届出の投函をもって運用開始とする決定のため。**投函後に状態を「巡回中」へ戻すだけで有効化**できる。
  **福祉新聞も照会待ちで保留**（同じ手順で復帰）。
  団体は**行政と同じ扱い**（要約・重要度・分野タグ・締切抽出・件数・グラフに合流）で、
  区別は印章［団］だけ。全国保育士会は調査のうえ**不採用**、全国保育協議会は**保留**。
- 公開ダッシュボード **https://kanda-houtokukai.github.io/fukushi-watch/**（新聞紙面構成）:
  題字＋欄外（和暦・創刊から第N号／創刊前は「創刊前の記録」）・PC2カラム／スマホ1カラム＋
  固定ヘッダーとドロワー・トップ記事＋高中低の濃淡・低は索引・報道欄・検索・分野切替・
  「この7日」（対応が要る日の地図）・「期限が近いもの」・全項目に共有（Web Share／URLコピー）・
  **報道ティッカー**（題字直下・その日の報道を流す・0件の日は非表示）。
  重要度と分野のチップは**PC・スマホとも紙面側**（押した結果が目の前で変わる位置）。
- **助成で稼働しているのは5口**（JFC・中央共同募金会・福岡県共同募金会・WAM助成・
  中央競馬馬主社会福祉財団=P27）。馬主財団は締切が構造的に取れない源で、**「締切不明」の束に入る
  （それでよい・P27の[DECISION]）**。
  ⚠️**全社協ハブは「保留（届出前）」で停止中**——届出が未投函のため（2026-08-25に是正）。
  **jyosei-navi も「保留（照会前）」**で実装済み・未稼働（承諾後に1行で有効化）。
- **助成面（P24）**: 題字下のタブで紙面と切り替え。`data/grants.json`（いま応募できる助成）を
  締切あり／随時・通年／締切不明の3束で表示。分野（単一）×用途9値（複数OR）×応募主体トグルで絞る。
  共有は `?v=grants&g=hash8`。**紙面からの導線は「期限が近いもの」欄の下に1本**。
  絞り込みは **分野（単一）× 用途（単一・P25で複数選択から変更）× 応募主体トグル**、いずれもAND。
  用途に「すべて」のチップは無く、**同じチップをもう一度押すと解除**（選択中は「×」が出る）。
  各項目に**「応募の条件」のタップ展開**（P28）: 募集ページからの**逐語引用**を9軸で並べる。
  AIは「どこが条件か」を選ぶだけで表示は原文（判定はしない・できない構造）。
  空＝「条件の記載を確認できませんでした」の正直表示＋必ず募集要項への導線。
- 履歴は**40日分・633件（高71）**（2026-07-01〜）。P14で7/1〜8/15の547件を遡及記録し、
  **関連記録の系列照合が効いている（related 117件）**。
- 分野タグは**機械的に正規化**（4分野すべて／共通と個別の混在 → `["共通"]`。P15）。
- 欄外は**常に本日**の日付と号数を示す帯（P22。閲覧中の紙面の日付は日付見出しに出す）。
  **過去を見ているときだけ欄外の日付が「本日へ戻る」ボタンになる**（本日なら押せない）。
  スマホは中間形「令和8年8月25日（火）」・PCは「2026年（令和8年）8月25日　火曜日」。
- 件数表示は**PCが内訳あり／スマホは「新着N件」のみ**（P22）。分野で絞り込み中の表示は両方で維持。
- 原本への導線は**見出しリンクに一本化**（P22。「原本を確認する →」は廃止）。
- スマホは**文字サイズを3段階**（標準/大/特大・メニュー内・localStorage記憶）。拡大は本文系のみ。
- 運用コストは完全ゼロ（Gemini無料枠・Actions無料枠）を維持。

## 次の一手

- **助成財団センターへの照会送付（ユーザー作業）。** 文面は
  `~/Documents/fukushi-watch-taigai/jfc-navi-api-shokai-2026-08-25.html`。
  **推奨はウェブフォーム**（`https://www.jfc.or.jp/contact/` の「助成団体ポータルサイト“助成・奨学金情報navi”」）。
  **承諾を得たら** `docs/sources.md` の #11 を「巡回中」に1行変えるだけで有効化できる。
- **助成の観察**（1〜2週間の実運用で見る）:
  ①**WAM助成が募集期に0件から立ち上がるか**（年1〜2回。0件が続くのは正常だが、
  募集が始まっても0件のままなら見出しの絞り込み条件が狭すぎる）
  ②**共同募金会の締切表記の揺れ**（【…M/D締切】【…M/D必着】以外の書き方が出ないか）
  ③**重複統合の誤爆がないか**（件数が静かに減るだけなので気づきにくい。
  同じ財団の別助成が消えていないかを目で見る）
  ④**JFCの福祉外除外の精度**（毎回2件前後が落ちている。福祉の助成まで落ちていないか）
- **全社協への届出の投函（ユーザー作業）。** 投函後に `docs/sources.md` の
  **#9と#10の2行とも**「保留（届出前）」→「**巡回中**」へ変更してコミット・プッシュすれば運用開始。
  ⚠️**必ず2行そろえる**（届出は全社協サイトへのリンク設置についてのもので、
  新着情報も助成情報も同じ対象。片方だけ動かすと届出前の運用になる）。
  投函後に `node scripts/grants.js` を1回走らせれば助成7件が戻る。文面は
  `~/Documents/fukushi-watch-taigai/zenshakyo-link-todoke-2026-08-21.html`（ブラウザで開いて印刷）。
  **差出人（住所・氏名・連絡先）と投函日は差し替えが要る。** 返事は待たない運用のため、
  投函後にすることは無い（`sources.md` は既に「巡回中」）。
- **P21の初回巡回の見届け。** 初回はベースライン保存のみで新着0件（設計どおり）。
  **団体の項目が紙面に出るのは2日目以降**。翌朝のActionsで［団］印章と判定を実データで確認する。
- **全社協の過去分のbackfill（投函後に実行）。** 改修は済み・下見で9件を確認済み。実行は:
  `node scripts/backfill.js --from 2026-07-01 --to 2026-08-20 --ignore-progress`
  （6日分・9件・6リクエスト。⚠️`--ignore-progress` を外すと5日分が抜ける）。
  実行後は `node scripts/relink.js` で関連記録を付け直す（P14と同じ手順）。
- **福祉新聞への問い合わせ送付（ユーザー作業）→ 回答を待つ。**
  掲載可なら `docs/sources.md` の福祉新聞行を「巡回中」へ戻すだけ（コード変更不要・
  stateのベースラインも温存済み）。不可なら「掲載不可」に更新して終了。
- 上記以外は指示書待ち（停止中）。
- 観察ポイント（1〜2週間の実運用で見る）:
  ①メール分量 ②「高」インフレ（P6の中立化で改善傾向）③分野タグの精度（特に「共通」）
  ④対応の目安（action）の距離感と品質 ⑤締切ウォッチが対象のある朝に回るか。


## 生きている注意事項

- ★ **鍵類の値を、リポジトリ・台帳・チャットに絶対に書かない。** Gemini API キーと
  Gmail アプリパスワードは **GitHub Secrets にのみ**保管する。文書に書いてよいのはシークレット名まで。
- ★ **運用コストは完全ゼロを維持する**（Gemini API 無料枠・GitHub Actions 無料枠）。
  課金が発生する選択肢は、採用前に必ずユーザーに確認する。
- ★ **差分スナップショットに監視先ページの全文をコピーして保存しない。**
  将来にわたって、保存するのは**ハッシュ＋見出し等の最小限**にとどめる。
  理由: ①官公庁ページの全文保存は著作権上の配慮が要る ②全文を貯めるとリポジトリが肥大し、
  差分の確認そのものが重くなる ③変更の検知に必要なのは「変わったか」と「どこが変わったか」であり、
  本文の複製ではない。**原本への直リンクを常に残し、最終判断は原本確認に誘導する**（dev-workflow 鉄則5）。
- ★ **外部の鍵・アカウントが要るフェーズは、指示書の冒頭に「手順0」を置く**
  （設定の雛形を作る → 入手先を案内する → いったん停止して待つ）。ユーザーに準備を先回りさせない。
- ★ **依存ゼロ構成を維持する**（npm パッケージを足さない。HTML解析も正規表現・SMTPも自作）。
  Actions で `npm install` が不要になり、壊れる面が少ない。複雑なHTMLの源が増えたら再検討可。
  ※アーカイブへ移したP1の決定だが、いまも全スクリプトを縛る制約のため昇格。
- ★ **抽出0件は「差分なし」でなく構造変化の疑いとして失敗扱いにする**（サイレント0件の禁止）。
  源ごとに失敗を隔離し、全滅のときだけエラー終了する。※同上（P1）。
- ★ **AIのモデル名を固定で書かない**（一覧取得→新しい順に試行→成功モデルを記録して次回優先）。
  **応答は件数一致・語彙・形式を必ず機械検証**し、落ちたら次のモデルで再試行、全滅なら失敗終了。
  ※同上（P2。世代交代404と件数抜けへの構造的対策）。
- ★ **項目ハッシュは「タイトル＋日付＋URL」**（URLだけにしない。同一URLへの繰り返し掲載がある）。
  ※同上（P1）。
- ★ **daily.yml の順序は 巡回→判定→送信→履歴→締切→状態コミット。状態コミットを送信より後に置く。**
  送信に失敗した日は状態が更新されず、翌朝に同じ新着を再検知して送り直せる（取りこぼさない構造）。
  ※アーカイブへ移したP3の決定だが、順序を変えるとこの保証が壊れるため昇格。
- ★ **Actions の cron は指定時刻ちょうどには動かない**（数分〜十数分の遅延が正常）。
  「7時を過ぎたのに来ない」はまず遅延を疑い、Actions の実行履歴を見る。※同上（P3）。
- ★ **運用の鍵の落とし穴（再発しうる）**: Gmail SMTP は**そのGoogleアカウントにGmailアドレスが
  存在すること**が必要（外部アドレスのログインIDでは 535 になる）。`.github/workflows/` を push する
  トークンには **`workflow` スコープ**が要る（`repo` だけでは拒否）。※同上（P3）。
- ★ **ファイルの追記は、読み込みと書き込みを1つの式にまとめない。**
  Pythonの引数評価順などにより、**読む前に書き込みモードでファイルが開かれて中身が消える**
  （実例: `open(p,'w').write(open(p).read()+追記)` でアーカイブが空になった）。
  **読み切って閉じてから、改めて書く**手順にする。リポジトリ管理下なら git から復元できるが、
  **`data/` 配下の未コミットファイルは復元できない**（state.json・履歴を扱うときは特に注意）。
- ★ **ローカル検証と Actions の自動コミットは衝突する**（data/ の差分）。ローカルの未コミット
  data/ は `git checkout -- data/` で捨て、push 拒否は `git pull --rebase` で取り込む。
  **Actions 側が常に正**（そちらが新しいため）。※アーカイブへ移したP5の教訓だが再発頻度が高く昇格。
- ★ **判定の骨格（P6で確定・いまも全機能の前提）**: 分野タグの語彙は**保育/障害/高齢/児童/共通**の
  5値＋空配列（＝福祉と無関係）。**迷ったら空でなく共通に倒す**。重要度は**分野中立で1つ**だけ付け、
  受け手ごとの調整は分野タグで行う（メールは `data/mail-settings.json` の重み付け、
  ダッシュボードは動的な絞り込み）。**25件超は複数リクエストに分割**する。
  読み手は **fields の欠落を空配列として扱う**。※アーカイブへ移したP6の決定だが全機能の土台のため昇格。
- ⚠️ **jyosei-navi は網羅のベースではない。** 検索APIは**募集期間が登録された現役の公募のみ**を返し、
  **主要な企業財団が検索に出ない**。P24-3で保存した福祉110件を再検証した実測（2026-08-25・
  新規リクエストなし）:
  **出てこない** → 洲崎福祉財団・大和証券財団・SOMPO福祉財団・松翁会・みずほ教育福祉財団・
  日本生命財団・キリン福祉財団・三菱財団・中央共同募金会
  **出てくる** → 日本フィランソロピック財団・齋藤茂昭記念財団・松の花基金
  ⇒ **JFC新着RSS＝フロー／jyosei-navi＝ストック の併用が前提**で、
  **naviはRSSの上位互換ではない**。「naviに主要どころが入っている」は誤り。
  ⚠️ **全社協ハブ（`/guide/sponsor/`）が企業財団系の穴埋めそのもの**であり、
  **届出の投函が網羅性の最優先事項**である（上に挙げた9つのうち大半は全社協ハブに載る）。
- ⚠️ **jyosei-navi API へは、助成財団センターの承諾を得るまでリクエストしない**（ユーザー指示・2026-08-25）。
  照会文面に「毎朝1回・最大21リクエスト」と書いており、承諾前の追加アクセスは約束と食い違う。
  検証が要るときは **P24-3で保存した110件のスナップショット**を使う。
- ⚠️ **backfillで積んだ項目には締切ウォッチ（P11）が走らない。**
  遡及分は**掲載日の紙面**に入るため、締切のある項目が含まれていても
  **「期限が近いもの」欄には出ない**（`deadlines.js` の入力は当日の `report-latest.json` のみ）。
  P14で「遡及分に締切抽出はしない」と決めたのは**期限切れが大半という前提**だったが、
  **P26では遡及範囲に有効な締切が含まれた**（介護ＤＸ支援事業費補助金・8/31締切を8/3の紙面に記録）。
  将来、**遡及分にも締切抽出を通すかを再検討する余地がある**。
- ★ **年が省略された日付表記（M/D）は、掲載日を基準に解く。**
  「今日以降でいちばん近い年」で解くと、更新されない古い記事の締切が翌年に繰り上がり、
  **締切済みのものが未来の予定として並ぶ**。しかも未来日なので**期限切れ削除にも引っかからず、
  静かに間違ったデータが残り続ける**（P24-4で福岡県共同募金会の締切済み2件が2027年として出た）。
  掲載日基準で解けば過去日になり、正しく落ちる。
- ★ **重複の統合は、条件を緩くすると別物まで畳む。**
  **同一の財団が同じ締切日で複数の助成を出すことは実際にある**（SOMPO福祉財団の「組織強化」と
  「認定NPO取得」はどちらも締切2026-10-09）。統合の判定には必ず
  **助成そのものを特定できる条件（締切の一致＋名称の包含関係）**を使い、
  **出し手名だけでの照合は「片方が空のとき」に限る**。緩い照合は件数が減るだけで気づきにくい。
- ★ **NFKC正規化を通したテキストに、全角のままの正規表現を書かない。**
  `normalize("NFKC")` は `（）`→`()`、`～`(U+FF5E)→`~`(U+7E) に変える。全角で書くと一致せず、
  **黙って全件が「取れなかった」になる**（P24-4でJFCの締切が全件unknownになった）。
  波線は U+7E / U+FF5E / U+301C の3つを許すこと。
- ★★ **源の性質や状態を、コードに埋め込まない。必ず台帳（`docs/sources.md`）を参照する。**
  実例3件:
  ① `backfill.js` が**報道源を名前で列挙**していた（P24-4）
     → 報道源を1つ足すとこのスクリプトだけ取り残され、**報道にAI要約を付ける**ところだった
  ② `backfill.js` が**全社協を無条件に取得**していた（P26）
     → 届出前で「保留」にした源を、**遡及だけが取りに行く**ところだった
  ③ `backfill.js` が **P15の正規化を持っていなかった**（P21）
     → 生の `fields` を書き、遡及分だけ正規化から漏れるところだった
  ⚠️ **いずれも `backfill.js` で起きている。** 本流（`crawl.js` / `summarize.js`）に入った判断が
  単発スクリプトに届かない構造が繰り返し露見している。
  ⚠️ **「止める」という判断は、毎朝の巡回だけでなく、遡及・再計算・正規化のすべての経路に
  等しく効かなければならない。** 新しい単発スクリプトを作るときは、
  **本流が持っている判断（台帳の状態・区分・正規化）をすべて通しているか**を確認すること。
  ★ 点検の実施（2026-08-25）: `relink.js`・`normalize-fields.js`・`deadlines.js`・`grants.js` を確認。
  前3つは**取得を伴わず既に記録済みのデータだけを扱う**ため台帳参照は不要（`deadlines.js` の入力は
  `report-latest.json` で、そこに至る時点で台帳の絞り込みを通っている）。`grants.js` は
  `readSources()` で区分と状態を見ており正しい。**停止中の源のデータ残留も0件**
  （履歴・`deadlines.json`・`grants.json` の全てを機械照合）。
- ★ **団体源(P21)には、他源に無い「原本の禁止文言チェック」が挟まっている**（`checkReprintNotice`）。
  全社協への届出で約束した運用のため、**外してはいけない**。⚠️取得失敗を「除外」にすると
  一時的な通信障害で項目が永久に消える（state に載って二度と再評価されない）。
  失敗は必ず**保留＝stateにも載せず翌朝やり直す**側に倒す。新しい団体源を足すときも同じ。
- ★ **対外文書（届出・照会状）はリポジトリに置かない。** 置き場は `~/Documents/fukushi-watch-taigai/`。
  公開リポジトリに個人の住所・氏名が入る事故を構造的に防ぐ。
- ★ **プレビューサーバは `~/Documents` 配下を読めない**（全て404になる）。実機確認は
  スクラッチパッドへ `index.html` と `data/` を複製して配信する。検証用にデータを細工したら
  **`git checkout -- data/` で必ず復元する**（P16の作法）。
- ★ `docs/sources.md` の**確定済みの行は承認なしに書き換えない**。URLの確定はP1（1源目）・P4（残り4源）。
- ★ 報酬改定関連は **報酬改定ナビ**（`~/Documents/houshu-navi/`・kanda-houtokukai/houshu-navi）と
  情報源が重なる可能性がある。突き合わせて重複監視を避ける。
- ★ **本案件は「個人の試験開発」**。法人PT案件ではないため `references/pt-context.md` は適用しない。
  ただし**形になったら PT・県協会への提示物に昇格**する想定なので、他人が見る前提の作りにしておく。
- ★ **停止規律（P9の教訓・2026-08-18）**: 指示書に「区切り②は区切り①の承認後のみ」とある場合、
  **その区切りの承認を明示的に確認してから次へ進む**。選択肢の提示に推奨を添えて一括承認を得る形は、
  判断を先導して停止ポイントを実質すり抜けることがある。特に**著作権・対外関係など
  実装の妥当性と別軸の判断**が含まれる区切りでは、論点ごとに承認を分けて確認する。
- ★ **メールの分量（P5で実測・承認済み・引き続き観察）**: 5源の実測は**平日12〜18件/日**
  （当初懸念の30〜50件より少ない）。15件のメールをユーザーが確認し「このまま運用」で承認済み。
  それでも長いと感じたら: **高・中はメール本体として現状維持、低は圧縮**（件数の集約のみ等）。
  低の詳細は P4 ダッシュボードの役割とし、**メール＝判断が要るもの／ダッシュボード＝全件**の分担。
- ★ **「高」判定のインフレ傾向（観察中・P4以降で判断）**: 感染症の**定期更新データ**
  （コロナ定点報告の推移など毎週来るもの）まで「高」に判定される。基準の「感染症=高」に忠実な
  結果だが、毎週高が並ぶと件名の「高N件」が薄まる。1〜2週間の実運用で頻度を見て、
  プロンプトに「定期更新の統計データは中」等の微調整を検討する。

## ブロッカー

- なし（福祉新聞の照会待ちのみ。運用は継続中）。

## ファイルの地図

| ファイル | 役割 | 正本か |
|---|---|---|
| `fukushi-watch-handoff.md` | この台帳。引き継ぎの入口 | ✅ 正本 |
| `CLAUDE.md` | Code がセッション開始時に自動で読む案件固有ルール | ✅ 正本 |
| `docs/sources.md` | 監視対象台帳（源の名前／URL／巡回方法＝パーサ名／状態） | ✅ 正本 |
| `scripts/crawl.js` | 巡回＋差分検知（P1）。台帳の「巡回中」行だけを巡回。依存ゼロ | ✅ 正本 |
| `scripts/summarize.js` | Gemini要約＋重要度判定（P2）。diff→report。依存ゼロ | ✅ 正本 |
| `data/state.json` | 前回巡回時の項目リスト＋`preferredModel`（成功したGeminiモデル） | 実行で更新 |
| `data/diff-latest.json` | 直近実行で検出した新規項目。**P2の入力** | 実行で更新 |
| `data/report-latest.json` | 要約＋重要度付きの新規項目。**P3のメール本文の材料** | 実行で更新 |
| `.env.example` | 必要な環境変数の雛形（`GEMINI_API_KEY=`・値は空） | ✅ 正本 |
| `.env` | 鍵の実値。**git追跡外**（.gitignore済み）。値はどこにも書かない | ローカルのみ |
| `scripts/mail-template.js` | 件名とHTML本文の組み立て（P3）。P4ダッシュボードも同じ材料を使う | ✅ 正本 |
| `scripts/notify.js` | Gmail SMTPS(465)送信（P3）。依存ゼロの自作クライアント | ✅ 正本 |
| `.github/workflows/daily.yml` | 毎朝7時(JST)=22:00(UTC)の定期実行＋手動実行 | ✅ 正本 |
| GitHub Secrets | GEMINI_API_KEY / GMAIL_USER / GMAIL_APP_PASSWORD / MAIL_TO の4つ | 値はGitHubのみ |
| `scripts/archive.js` | 履歴蓄積（P4）。report を月次＋索引へ。同日はハッシュ併合 | ✅ 正本 |
| `data/history/` | `YYYY-MM.json`（日毎の全項目）＋`index.json`（日毎件数） | 実行で更新 |
| `data/mail-settings.json` | メールの分野重み付け設定（現在=障害・児童・保育） | ✅ 正本 |
| `index.html` | ダッシュボード本体（依存ゼロ・単一ファイル） | ✅ 正本 |
| https://kanda-houtokukai.github.io/fukushi-watch/ | 公開ダッシュボード（Pages・main/root） | — |
| `scripts/deadlines.js` | 締切抽出（P11）。本文取得→幻覚ガード三重検証→期限のみ保存 | ✅ 正本 |
| `scripts/grants.js` | 助成金の収集（P24・**紙面と別系統**）。区分=助成の源だけを扱う。締切は正規表現で抽出（AI不使用） | ✅ 正本 |
| `~/Documents/fukushi-watch-taigai/` | **対外文書（リポジトリ外）**。全社協あて届出／助成財団センターあて照会 | ✅ 正本 |
| `data/grants.json` | **いま応募できる助成**（締切超過は保存からも削除）。紙面のhistoryには積まない | 実行で更新 |
| `data/deadlines.json` | 追跡中の期限（期限日・種類・根拠文字列・hash） | 実行で更新 |
| `scripts/backfill.js` | 過去分の遡及記録（P14・**手動のみ**）。stateは読むだけ・メール送信なし。P21で全社協に対応（禁止文言チェック込み）＋重複判定を履歴全体に修正 | ✅ 正本 |
| `scripts/relink.js` | 全履歴の関連記録を再計算（P14・**手動のみ**・AI不使用） | ✅ 正本 |
| `data/backfill-progress.json` | バックフィルの処理済みの日（再開用） | 実行で更新 |
| `~/Documents/fukushi-watch-taigai/` | **対外文書（リポジトリ外）**。全社協あて届出の印刷用HTML | ✅ 正本 |
| `fukushi-watch-archive-2026-08.md` | 経緯アーカイブ（P0〜P24-4・P24-3の記録とロードマップP0〜P26の行を原文のまま保存） | 参照用 |
| `package.json` | npm run crawl/summarize/notify/archive の定義のみ（依存なし） | ✅ 正本 |
| `~/Documents/fukushi-watch/` | ローカルの作業場所 | — |
| `github.com/kanda-houtokukai/fukushi-watch` | 公開リポジトリ | — |
| `~/.claude/skills/dev-workflow/` | 作業規範（全案件共通） | ✅ 正本 |


---

# 決定事項（[DECISION]）

## 2026-08-25（P24-2・助成面）

- **[DECISION] 面の切り替えは題字の下のタブ**（紙面／助成）。⚠️**固定ヘッダー(#topHead)の外**に置く——
  中に入れるとスマホの固定ヘッダーの行高（欄外34px＋題字52px＝86px）が崩れる（P10の保証）。
  タブに助成の件数を添える（`助成 7`）。助成面では**報道ティッカーを隠す**（紙面の情報のため）。
- **[DECISION] 出し分けは `body[data-view]` でCSSだけで行う。** 描画ロジックを二重化しない
  （P10のrelocate・P22のtallyと同じ型）。`<body data-view="paper">` をHTMLに書いておき、
  JSより前に効かせて助成面のちらつきを防ぐ。
- **[DECISION] 用途タグは複数選択（OR）にする。**
  → ⚠️**P25で単一選択に変更した**（2026-08-25）。理由: 実際に使ってみて、
  **選んだものが残り続けて解除しにくかった**。複数選択には「すべて」に相当する解除口が無く、
  選んだチップを1つずつ押し戻す必要があり、絞り込みの出入りが重かった。
  「車両か機器・用具のどちらでもいい」という探し方を想定してORにしたが、
  実際には**軸を1つに決めて見る**方が使われた。詳細はP25の記録。
- **[DECISION] 分野タグは紙面と助成で同じ `state.field` を共有する。**「うちは障害分野」という
  識別は面をまたいで変わらないため。どちらで変えても両方が追随する（戻ったときに食い違わない）。
  ⚠️用途タグと応募主体トグルは助成面だけの状態（localStorageに保存）。
- **[DECISION] 朱は「残り14日以内」に使う。** 紙面の期限欄（3日）より広いのは、
  助成は申請書の準備に日数が要るため。**新しい色は持ち込まない**（実測: 助成面で使っている色は
  墨・薄墨・極薄墨・紙・朱の5つのみ）。
- **[DECISION] 束は3つに分け、件数を必ず見せる**: 締切あり（近い順）→ 随時・通年 → 締切不明。
  絞り込み中は `7件 ── 締切あり6・随時通年0・締切不明1 ／ 全7件` の形（P19の作法を踏襲）。
- **[DECISION] 紙面からの導線は1本だけ**——「期限が近いもの」欄の下に
  「応募できる助成 N件 →」。⚠️新しいボタンを増やさない（P17の考え方）。0件のときは出さない。
- ★ 共有リンクは **`?v=grants&g=hash8`**（紙面は `?d=&i=`）。同じ共有ボタンで面によりURLを出し分ける。
- ★ 検証: 用途タグの複数選択がORで効き（研究・調査1件→＋機器・用具2件）、応募主体トグルと
  分野がANDで効く（2件→1件→0件）ことを実測。全7件に元ページ直リンク・共有・印章・金額あり。
  320/375/1280px × 文字サイズ3段階で横スクロールなし、固定ヘッダー86pxも不変。
- ★ 教訓（環境）: **プレビューの `resize_window` は `resize` イベントを発火しない**ため、
  幅を変えても `relocate()` が走らずサイド欄がドロワーに残る。**実装の不具合ではない**
  （`dispatchEvent(new Event('resize'))` で正しく戻ることを確認）。鉄則4の切り分けの実例。

## 2026-08-25（P24・助成金）

- **[DECISION] 助成金は紙面（history）と完全に別系統**にする。書き先は `data/grants.json` ただ1つで、
  「いま応募できる助成」だけを持ち、締切が過ぎたものは表示からも保存からも消す
  （`deadlines.json` と同じ思想）。理由: ①**時間の向きが逆**（紙面は過去に積み上がる／助成は
  締切に向かって減る）②同じ助成が数か月間ずっと有効で、日ごとに記録すると同じものが何度も並ぶ
  ③件数集計とグラフ（この7日＝対応が要る日の地図）の主語が壊れる。
  ⚠️`deadlines.json` とも分ける。あちらは「行政通知に**付随する**期限」、こちらは「助成**そのもの**」。
- **[DECISION] 締切の抽出にAIを使わない。** 助成の源はどれも締切が**ラベル付きの定型フィールド**
  （全社協=`（情報掲載…/募集締切…）`／JFC=`応募期間（締切）`／jyosei-navi=`enddate`／
  共同募金会=見出しの【…8/21締切】）で、正規表現で取れる。**AIを介さないので幻覚が原理的に起こらない**。
  P11から流用するのは**日付パーサ（和暦・全角・年なし対応）と範囲チェックだけ**。三重検証は不要。
  AIを使うのは 要約・分野タグ・用途タグ・応募主体タグ の付与のみ。
- **[DECISION] 締切は3つの型に分ける**: `date`（日付が定まる）／`rolling`（随時・通年）／
  `unknown`（「11月中旬」等）。既定は**締切ありを締切順**、随時・通年は別の束。
  ⚠️**隠さず件数は見せる**（jyosei-naviでは福祉110件の**65%が随時・通年**で、隠すと大半が消える）。
- **[DECISION] 用途タグ9値**（実データ134件＝jyosei-navi110＋全社協14＋JFC10 から起こした）:
  **設備・改修／機器・用具／ICT／車両**（モノ・ハコ系）＋
  **人材・研修／事業・活動／研究・調査／行事・啓発／表彰**（ヒト・コト系）。空配列＝読み取れない。
  畳まない理由（ユーザー判断）: 設備投資系をまとめると「車両を探している」人が施設改修まで見ることになる。
  ⚠️**災害対応は入れない**（使途でなく**目的**。「災害対策品の購入」は機器・用具に収まる）。
  ⚠️**奨学は入れない**（使途でなく**応募主体**の軸）。
- **[DECISION] 応募主体タグ5値**: 社会福祉法人／NPO・公益法人／任意団体／個人／研究者・大学。
  ⚠️**空配列は「誰でも可」ではなく「読み取れない」**。UIで「記載なし」と出すこと。
  研究助成も**落とさずそのまま出す**（大学との共同研究・施設の実践研究があり、法人が応募できないとは
  限らない。取り込み時に落とすと二度と拾えず、既定で除外すると存在に気づけない）。
- **[DECISION] 用途タグの正規化は分野タグと逆向き**にする（P15の応用）。
  分野の「共通」は「**全分野に効く**」という強い意味なので個別を吸収するが、
  用途の「事業・活動」は「**その他・一般**」という弱い受け皿なので、**具体が付いたら受け皿を落とす**。
- ⚠️ **正規化の閾値「6値以上で空配列」は未検証**。「使途を問わない助成」を弾く規則だが、
  実測は洲崎3値・松翁会4値どまりで6値の実例がまだ無い。**AI付与を何度か走らせた実測で確定すること**。
  それまでは事実上ほぼ発火しない規則である点に注意。
- ★ **既存6源には助成の公募が実質流れていない**（履歴675件を機械照合し3件、うち2件は調達・科研費）。
  独立した源が要ることが実測で裏づけられた。重複の心配は不要。
- ★ **用途タグは「タイトル＋要約」からは付かない。** jyosei-naviの`PURPOSE`は**目的**であって使途ではなく、
  使途が明示されるのは**個別ページ本文**（全社協は14件中12件に「助成対象」欄、洲崎は「対象種別 車両購入、
  物品購入、施設工事」、松翁会は「施設備品やICT化促進の物品…施設改修」）。**本文を材料にすること。**
- ★ 検証: 実データで **洲崎→設備・改修＋機器・用具＋車両／松翁会→設備・改修＋機器・用具＋ICT＋行事・啓発／
  SOMPO自動車購入→車両** が正しく付いた。ユーザーの3例（ICT・福祉用具・建て替え）はすべて拾える。
  応募主体も **SOMPO自動車購入＝NPO・公益法人のみ**（社会福祉法人は応募不可）を正しく分離できた。
- ★ **ハブ型の削除規則**: 一覧から消えた＝募集終了として `grants.json` からも消す。
  ⚠️ただし**源の取得に失敗した回は消さない**（全消えの事故を防ぐ）。
- ★ **全社協ハブの個別ページにも `checkReprintNotice` を通す**。届出で約束した除外は
  助成情報にも及ぶため（新着だけの約束ではない）。

## 2026-08-25（P25・用途タグの単一選択化）

- **[DECISION] 用途タグを複数選択（OR）から単一選択に変更する**（P24-2の判断4の変更）。
  経緯: **実際に使ってみて、選択が残り続けて解除しにくかった**。複数選択では
  「すべて」に相当する解除口が無く、選んだチップを1つずつ押し戻す必要がある。
  想定していた「車両か機器・用具のどちらでもいい」という探し方より、
  **軸を1つに決めて見る**方が実際には使われた。分野タグと挙動をそろえた方が学習も要らない。
- **[DECISION] 用途に「すべて」のチップは置かない**（ユーザー判断）。分野は5値なので
  「全分野」を置けるが、用途は9値あり、10個目のチップは行を増やして重くなる。
  解除は**同じチップをもう一度押す**。
- **[DECISION] 解除の手がかりは「選択中のチップにだけ × を出す」形にする。**
  ⚠️説明テキストは常時出さない——**選んでいないときは何も足さない**。
  ×は選んだ瞬間にだけ現れるので、押した本人の視界に必ず入る。色は薄墨（`--sub`）で、
  「削除」でなく「解除できる」と読める重さにした。新しい色は持ち込んでいない。
  ⚠️分野チップには×を出さない。あちらは「全分野」で解除できるので手がかりが要らず、
  **見た目の差が挙動の差をそのまま表している**（同じに見せる方がむしろ嘘になる）。
- ★ 保存形式を配列から単一値に変えたので、**旧形式（配列）が入っている環境では先頭の1つを
  引き継ぐ**ようにした。黙って設定が消えると、使う人には不具合に見える。
- ★ 検証: 選び直し（事業・活動→表彰）・同じチップでの解除・分野/応募主体とのAND
  （13→5→4→1件）・件数表示の追随・旧形式からの引き継ぎ（`["ICT","車両"]`→ICT）を実測。
  PC/スマホ両方、文字サイズ3段階で横スクロールなし・固定ヘッダー86px不変。紙面側は無影響。

## 2026-08-25（P25の後・台帳の維持と全社協の是正）

- ★ **全社協の状態の食い違いを是正した。** #9（新着情報）は「保留（届出前）」なのに
  #10（福祉の助成情報）を「巡回中」にしていた。**届出は全社協サイトへのリンク設置についてのもので、
  新着情報も助成情報も同じ対象**なので、片方だけ動かすと**届出を送る前に運用を始めたことになる**。
  P24で「全社協ハブ（手続き済み）」という前提で進めたのが誤りだった（文面を作っただけで未投函）。
  ⚠️`docs/sources.md` に **#9と#10の状態は必ず一致させる**旨を注記し、片方だけ変えられないようにした。
- **[DECISION] 届出前に取得した全社協の助成7件は `grants.json` から削除する。**
  理由: ①約束は「送った時点で運用開始」であり、**送る前に公開している状態そのものが約束に反する**。
  源を止めるだけでは公開済みの7件が残る ②**ハブ型なので取り直せる**——投函後に1回実行すれば
  同じ36件から7件が1分で戻り、抱え込む理由がない ③**仕組み側も同じ結論**——保留にした源の項目は
  次回実行で `seenThisRun` に無いものとして自動削除される。手で消すのはそれを前倒しするだけ。
- ★ **教訓: 「手続き済み」という言葉を、確認せずに実装の前提にしない。**
  文面を作った段階と、投函した段階は別。**台帳の状態欄（`sources.md`）が唯一の真実**であり、
  会話の中の表現と食い違ったら**状態欄の方を信じて確認に戻る**。
- **[DECISION] 台帳の維持規則に条項を追加した**（CLAUDE.md）。
  記録が規定数内でも3.2万文字を超える場合は、**ロードマップの完了済みの行を原文のまま
  アーカイブへ移す**。⚠️決定の経緯と「生きている注意事項」は圧縮も移設もしない
  （前者は判断の理由が失われ、後者は毎回読まれることに意味があるため）。
  実施: P0〜P20の22行を移設し、**32,029字 → 29,872字**。
  ⚠️ロードマップは「どのフェーズで何をしたか」の唯一の索引なので、
  **アーカイブ側に見出しを付けて索引として残し、台帳側には移設先を指す一文を置いた**。

## 2026-08-25（P26・自治体の補助金）

- ★ **福岡県に福祉分野の補助金専用ページは存在しない。** 補助金は**分野別の更新一覧に流れる**ので、
  **監視するカテゴリを増やすことが補助金を拾うことに直結する**（農林水産には「補助事業・制度資金」が
  あるが福祉には無い）。P24で「福岡県は助成の公募が実質ゼロ」と報告したのは、
  **監視していた2カテゴリだけを見た結果**だった。
- **[DECISION] `/life/3/25/`（介護・高齢者福祉）を紙面に追加**（2026-08-25）。**監視の穴だった**。
  そこに**介護ＤＸ支援事業費補助金（ICT導入補助）**と**外国人介護人材確保強化事業費補助金**があった。
  既存の `fukuoka-life` パーサが同一構造を扱えるため**コード変更なし・台帳に1行**（設計どおり）。
  ⏸ `/life/3/28/`（その他社会福祉）は補助金0件・啓発月間や表彰式中心のため見送り。
- **[DECISION] 自治体の補助金は「助成」区分に入れず、紙面の行政情報として扱う。**
  理由: ①情報の型が紙面に合う（新着として流れる）②**補助金の情報は締切だけではない**——
  交付要綱の改正・様式変更・Q&A追加も同じ流れに乗るので、**締切のあるものだけを切り出すと
  残りが行き場を失う** ③締切はP11の締切ウォッチが拾う。
  ⚠️**両方に出す設計は採らない**。同じ補助金が2面に出ると、どちらが正か・両方対応が要るかが読めなくなる。
- **[DECISION] 福岡市・北九州市（政令市）は当面見送り。⚠️調査漏れではなく判断。**
  福岡市の「リンク・著作権等」に**「トップページ以外の各個別のページへのリンクについては、
  各ページに記載の担当課までお問い合わせください」**という定めがある。当サイトは記事ごとに
  原本へ直リンクする作りなので正面から当たり、しかも**担当課がページごとに違う**ため
  1通の届出では済まない。robots.txt は許可・HTML版サイトマップもあり**技術的には取得できる**が、
  手続きが現実的でない。北九州市は robots 許可だが**政令市を片方だけ入れる意味が薄い**ため同時保留。
  再検討するなら両市をそろえて判断すること。
- ★ **既存6源に施設向け補助金の公募は流れていなかった**（履歴675件を照合。「補助金×公募」の形は5件だが
  中身は**国庫補助協議・内示**（自治体向け事務）・**補助事業者の公募**（受託側の募集）・科研費で、
  施設が直接応募できるものは0件）。制度情報は流れるが公募案内は流れない、という構図。
- ★ backfill実測: 2026-07-01〜08-24 で**23件**を記録（うち補助金2件は**どちらも「高」**と判定）。
  一覧40件のうち範囲外16件（最古2022-02-21）は自動的に除外された。
  ⚠️**福岡県の一覧は「更新日」順の全件リスト**で新着リストではない（P14で既知）。
  一覧の件数から更新頻度を推定してはいけない。実流量は履歴側で測る。
- ⚠️ **介護ＤＸ支援事業費補助金は申請受付が令和8年8月31日まで**（P26調査時点で残り6日）。
  遡及で8/3の紙面に入ったため「期限が近いもの」欄には出ない（下の★の論点を参照）。
- ★ ［将来案］**厚労省の雇用関係助成金**。キャリアアップ助成金・人材確保等支援助成金
  （介護福祉機器助成コース）等は**福祉法人が実際に使う制度で価値が高い**が、
  ①**ハブ型の実装が要る** ②**通年制度で締切がない**ため助成面（締切順）にも紙面（新着）にも馴染まない。
  「**制度の一覧として持つ**」第三の形の設計が要る。
  P23で残した「新着として流れないページの中身を監視する型」と同じ実装が土台になる。
  ⚠️実測: 厚労省の履歴426件に**これらの助成金名は1件も出ていない**
  （`/stf/.../kyufukin/` の静的ページにあり、締切の記載も0件）。
- ★ JEED（高齢・障害・求職者雇用支援機構）は robots 許可・転載制限なしだが、
  **障害者雇用関係の助成金は通年制度で締切が無く**、RSSも無い。優先度は低い。

## 2026-08-25（P27・公営競技系の助成）

- **[DECISION] 中央競馬馬主社会福祉財団を助成の源として採用**（ユーザー決定・2026-08-25）。
  ⚠️**「記事本文が実質空」という点は、P21の全国保育士会・P23の福岡県社協（いずれも不採用）と
  同じだが、判断が異なる。基準が緩んだのではない**:
  - 過去2件の不採用は「本文が空」に加えて**「内容が薄い」**ことが決め手だった
    （全国保育士会は実務価値が年3件で78%が会員限定、福岡県社協は77%が第三者評価の同型記事）。
  - 馬主財団は本文は空だが、**福祉車両・特殊浴槽等備品・増改築が社会福祉法人向けに出る**という
    **内容の適合度が今回の候補で最も高い**（助成面の中核用途に直撃する）。
  - **「締切不明の束に落ちる」ことは問題ではない**。締切不明でも存在を知ることに価値がある。
    年1回の助成を知らないまま過ごすより「これがある、詳細は窓口へ」と分かる方が実益がある。
- 原本確認（前セッションの未確認2件の決着）:
  - ✅**「総事業費の3/4補助」は原本で確認できた**。トップ階層には無いが、下層ページ
    `/reiwa-8/reiwa-8-1` に「総事業費に対する助成限度額 総事業費の４分の３以内」と明記。
  - ❌**「年1回・3月調査書→4月申請」は確認できず**（引き続き未確認）。むしろ申請ページ
    `/reiwa-8/reiwa-8-3` に「**申請の受付期間は馬主協会・共同募金会毎に異なりますので、
    直接問合せ下さい**」と明記されており、**全国一律の締切がそもそも存在しない**。
    締切が取れないのは記事の書き方の問題ではなく**構造的**——「締切不明」の束が正しい置き場。
- 実装（コレクタ `umanushi-rss`・最小限）:
  - タイトルで**募集・申請の案内だけ**に絞る（募集/申請/受付/公募 を含み、
    終了/決定/報告/監査/交付 を含まない）。実測: RSS10件中1件（施設整備等助成）だけが通り、
    研修の実施報告・交付決定・監査書式・**海外研修生募集（個人向け＝法人が応募する助成でない）**が落ちる。
  - **本文はリンク先ページを読んで補う**（記事内の同一ドメインの最初のリンク。記事自身・トップ・
    添付ファイルは除く＝助成事業ページに当たる）。このため `parseRssItems` に生のHTML
    （`html`）を追加した（他の源は不使用・挙動不変）。
  - 締切は「締切」ラベルのある文だけから取る（無ければ unknown）。国内研修助成の受付記事は
    本文に「申請受付の締切りは令和8年1月30日です」の形があり、将来の回は date で取れる。
  - `RSS_METHODS` に追加＝`checkReprintNotice` を通さない（あれは全社協への届出で
    約束したもの・P24-3の[DECISION]。無関係な相手に確認リクエストを撃たない）。
  - 印章は**団**（公益財団法人＝全国団体。JFC・共同募金会と同じ）。
- ★ 実測（2026-08-25）: raw10件→絞り込み後1件。判定は fields=共通／
  uses=設備・改修・機器・用具・車両／applicants=社会福祉法人・NPO・公益法人／締切不明。
  助成面の「締切不明」の束に表示され、用途=車両×応募主体トグルで正しく残ることをDOMで機械確認
  （このページはスクリーンショットが撮れない既知の挙動のため、確認はテキスト照合で行った）。
  ⚠️feedは直近10件保持のため、記事が流れ落ちた時点で項目は自動削除される（数か月先。
  年1回の助成なので、次の年度の申請案内が出れば新しい項目として入り直す）。

### 日本財団・JKA・NAR の原本確認（P27の区切り②・調査と記録のみ・実装なし）

- **日本財団**（確認済み）: ✅robots.txt 全許可・Yoastサイトマップあり。
  ✅**福祉車両配備助成 2026年度は 2026/5/18〜6/5 17:00 で受付終了**
  （`/grant_application/past` に明記）。⚠️**募集期間は年度でばらつく**
  （2024=7/1〜12・2025=7/1〜14・2026=5/18〜6/5。「毎年◯月」と固定できない）。
  ✅**本募集は毎年10月**（「毎年10月には、翌年の4月以降に実施される事業の…助成プログラムを公募」
  と明記。2026年度分＝2025年10月の募集は受付終了）。⚠️「2025/10/31締切」という具体日は
  原本で確認できず（未確認のまま扱う）。
  ✅**募集告知はニュースに流れず、Salesforceポータル（nippon-foundation.my.site.com/GrantPrograms）
  に載る**。`/feed` は**200を返すが本文0バイト**＝実質RSSなし。機械監視の口が無い。
  → sources.md に**季節リマインダー**（毎年9月中旬＋毎年4月下旬）を記録。源の追加はしない。
- **JKA**（確認済み）: ✅**福祉車両枠は本体と別日程・年2回**。2027年度は
  **第1回 2026/9/30 10時〜11/13 15時（事業者登録は11/6 15時まで）・
  第2回 2027/5/24 10時〜6/18 15時（同 6/11 15時まで）**
  （`hojo.keirin-autorace.or.jp/shinsei/shinsei_koueki_fukushi.html` に明記）。
  補助率3/4・対象に社会福祉法人・1法人1台・新車のみ・2025〜2027年度に交付決定済みの法人は対象外。
  ✅**本体の補助（2026/9/11締切）は jyosei-navi に載り、「※福祉車両の整備を除く」がタイトルに明記**
  （P24-3の110件スナップショットで確認・**新規リクエストなし**。JKAの2件はどちらも締切2026-09-11）。
  ⇒ navi有効化後、本体枠は自動で拾えるが**福祉車両枠はnaviに出ない**。日程を sources.md に記録。
- **NAR（地方競馬全国協会）**（確認済み）: ✅**補助事業は「畜産振興」「競走馬生産振興」の2本のみ**
  （`keiba.go.jp/association/livestock.html`。公募カテゴリは馬の改良増殖・畜産経営技術指導・
  畜産経営合理化・防疫衛生・その他畜産振興で、ページ内に「福祉」の語が0回）。
  **社会福祉向けの助成は現行存在しない**＝対象外として確定。
  中央競馬（JRA）系の社会福祉貢献は中央競馬馬主社会福祉財団（今回採用）が窓口であり、カバー済み。

## 2026-08-25（P28・応募条件の抽出）

- **[DECISION] 応募条件は「AIは選ぶだけ、表示は原文」で実装した**（区切り①でユーザー承認）。
  AIの出力は {axis, quote} で、**quote は募集ページからの逐語引用**。取得した本文に
  **引用が実在することを機械照合**し（空白を全て落として比較）、実在しない引用は捨てる。
  表示するのは quote そのものなので、**言い換えの誤りが構造的に起こらず、
  「応募できる/できない」の判定も構造的に不可能**。読み取れなければ空配列＝
  「条件の記載を確認できませんでした」と正直に表示する（推測で埋めない）。
  照合を1つも通らなかった項目は**1回だけやり直し**、それでも駄目なら「確認できず」。
- **[DECISION] 軸は9値**（実データ10件に実在した条件だけから起こした・机上で作らない）:
  所在地／**事業の実施地**（⚠️所在地とは**別軸**。アジア生協=団体は国内・事業地はアジア。
  混同すると誤るため軸ラベルで区別・ユーザー指示）／設立・活動年数／受給歴／併願・重複／
  補助率・自己負担／主体の付帯条件／提出書類／その他の対象限定。
  **法人の規模・職員数・事業所種別・定員は実データに無かったため軸にしない**（実例が出たら
  追加を判断。「収入1億円未満」のような規模条件は「その他の対象限定」が受けた実績あり）。
- **[DECISION] PDFは解析しない**（依存ゼロ構成の維持・幻覚リスク回避）。
  実測: 10件中9件は主要な条件がHTML平文にある。PDFのみは「詳細」の層（2件）で、
  展開内の「詳細は必ず募集要項でご確認ください」導線（財団側ページへの直リンク）で補う。
- **[DECISION] 抽出対象は conditions を持たない項目だけ**＝日常は新規のみ。導入時に1回だけ
  既存10件が自動で遡及された。**取得失敗は conditions を付けずに残し翌朝やり直す**
  （保留側に倒す・P21の失敗の扱いと同型）。上限は1回15件・AIは5件ずつ。
  コスト実測: 追加リクエストは新規1件につき要項ページ1回（1日平均1〜2回）。
  **再抽出しないので、100件超になっても日々のコストは新規流量にしか比例しない**。
- **[DECISION] 将来案（法人プロファイル×条件の突き合わせ）は見送り。
  再検討は、条件表示が実運用で安定してから、「注意チップ・既定オフ」の形でのみ行う**
  （2026-08-25・ユーザー合意）。理由: 機械照合で白黒付けやすいのは地域だけだが、
  その地域ですらSOMPOの**募集地域が年で東西交互**になる例や「所在地 vs 事業実施地」の
  混同で誤りうる。**「対象なのに非表示」の誤爆は、条件を間違える実害そのもの**。
- ★ **fetchHtml を文字コード対応にした**。`res.text()` は charset を無視して常にUTF-8で
  復号するため、Shift_JISのページが文字化けする（SOMPO福祉財団で実在）。
  **既存の全取得先（巡回中・保留とも）を点検し、全てUTF-8で実害なし**を確認したうえで、
  Content-Typeヘッダ→metaタグ→UTF-8 の順で復号する形に改修
  （締切抽出・禁止文言チェック・本文取得のすべての経路が恩恵を受ける）。
- ★ 教訓: **リンクを拾うときは「記事本文（content:encoded）」と「ページ全体のHTML」を
  区別する**。ページ全体には head のWordPress雑リンク（wlwmanifest.xml等）やナビが混ざる。
  馬主財団の条件ページ解決で実際に wlwmanifest.xml を掴み、RSS本文から選ぶ形に統一して修正した。
- ★ **潜在バグを発見して修正**: 共有ボタンの「コピーしました」ツールチップが `opacity:0`
  （**見えないがレイアウト上は存在する**）で隠されており、共有ボタンが右端に来た項目では
  ツールチップの幅ぶん**横スクロールが生まれていた**（375px・齋藤財団の項目で顕在化。
  **データ依存のバグで、P24-2の検証当時のデータでは出なかった**。変更前のindex.htmlでも
  再現することを確認済み＝P28の変更由来ではない）。`display:none`→copied時に表示へ修正。
  ⚠️ツールチップ表示中の1.6秒だけは従来どおりはみ出しうる（実害が小さいため許容と判断）。
- ★ 実測（2026-08-25）: 既存10件の遡及で**10/10件の抽出が完了**（引用の照合は全件通過）。
  JFC記事から財団側募集ページを選ぶ `pickFunderLink` は実データ8/8で正解。
  抽出例: 川野財団=所在地（埼玉・千葉・群馬・神奈川）＋受給歴／アジア生協=所在地・実施地・
  年数・受給歴・**収入1億円未満**／馬主財団=窓口（馬主協会・共同募金会）・対象・対象事業。
- ★ 検証: PC1280／375／320 × 文字サイズ3段階 × 展開/閉のすべてで横スクロールなし・
  固定ヘッダー86px不変。「確認できず」表示・ホバー規則（summary＝下線、要項リンク＝下線）・
  紙面側「対応の目安」の回帰・**条件UI内に判定めいた文言が無いこと**を機械確認。
  検証用のデータ細工は**プレビュー複製側のみ**で行い、実データには触れていない（P16の作法）。

## 2026-08-25（P29・助成面の密度とタブ）

- **[DECISION] タブは案A（現状の強化）**（ユーザー決定）: 選択中＝太字＋4pxの墨下線／
  非選択＝**細字の薄墨**。案B（墨ベタの白抜き）は新聞の面名標として正しい語彙だが、
  **白地に墨と朱の抑制された構成にベタ塗りが1箇所入ると重心が偏る**ため見送り。
  ⚠️差が弱いと感じたら**案Aの範囲内で**（文字をさらに太く・下線を太く）強めてよい。
  面が増えたら**折り返さず横スクロール**（`overflow-x:auto`・スクロールバー非表示・
  4面×320pxで折り返さないことを実測済み）。タブは固定ヘッダーの外＝86px保証に触れない。
- **[DECISION] PC項目は「タイトル主役・締切は右」に再配置**: タイトル16.5px太字、
  締切はタイトル右に nowrap（通常＝薄墨＋日付だけ墨太字／**残り14日以内は朱**＝既存の
  `.soon` の意味を位置だけ移した）。**年は今年と違うときだけ出す**（残り日数があるので
  年の取り違えは起きない）。締切不明・随時は薄墨の細字。
  ⚠️**スマホは従来どおり締切が上**——DOMは1つで、`.trow` の flex を
  `column-reverse` にするだけ（CSSだけで並べ替え・P10/P22の型）。
- **[DECISION] タグは案1**（ユーザー決定）: 枠を外し「ICT・車両<span>｜</span>社会福祉法人・NPO」
  の**中黒区切りの薄墨テキスト**に。**分野だけ既存の `.field`（色下線）を維持**して識別体系を守る。
  情報は削っていない（応募主体の記載なしも文字として残る）。`.gtag` 系のCSSと `tag()` は削除。
- **[DECISION] 用途チップはPCのみ余白9px・間隔6pxに詰めて9つを1行に**（文字12pxのまま）。
  スマホはタップ寸法を優先して現行のまま（アコーディオン内なので場所も取らない）。
- **[DECISION] スマホの絞り込みはアコーディオン（既定は閉）**。開閉は
  `body[data-gfilter]`＋幅のメディアクエリ＝**CSSだけの出し分け**で、resizeイベントに
  依存しない（P22の教訓）。**閉じていても絞り込み中は見出しに「絞り込み中: 障害・車両・
  社福のみ」を出す**。P19の件数表示は外に出したまま維持。
  ⚠️**紙面側の分野チップはアコーディオンにしない**（P10の判断のまま・回帰確認済み）。
- ★ 検証（2026-08-25）: PC1280／375／320 × 文字サイズ3段階 × アコーディオン開閉 ×
  条件展開で横スクロールなし・固定ヘッダー86px不変。絞り込みAND（車両1件→＋社福1件→復元10件）・
  締切順・共有・条件展開・紙面側（チップ常時表示・24項目・横スクロールなし）の回帰なし。
  朱は「残り6日」の項目にだけ出ることを実測。4面追加の320pxは横スクロールで続きを示す。
- ★ 教訓: **localhost の localStorage は過去セッションの検証状態を持ち越す**。
  絞り込みが効いたままに見える（10件が3件になる）のは不具合ではなくトグルの残留だった。
  検証で状態を触ったら `localStorage.clear()` まで含めて後始末する。
- mock-p29.html は**本体を実機確認してもらうまで比較対象として残す**（確認後に削除・ユーザー指示）。

## 2026-08-25（P30・助成面の視認性）

- **[DECISION] スマホの絞り込み見出しは地色（#f6f5f1）で周囲から分離**。
  低の索引・条件展開と同じ「操作できる箱」の色＝**新しい色ではない**。罫線は外し、
  押せる合図は地色＋▽で示す。「絞り込み中: …」の表示は維持。
- **[DECISION] 注意書き（AI参考情報の枠）は畳んだ**（位置は変えず・既定閉）。
  理由: ①紙面がAI注記を「このサイトについて」に畳んでいるのと同じ扱い
  ②各項目の「応募の条件」展開内に注記が既にあり、本文側の担保がある
  ③畳みを上部に残すことで**初見の導線**（「要約とタグについて（AIによる参考情報）▽」の1行）が残る。
  下部への移動案は「初見の人が読む前に通る場所」でなくなるため採らなかった。
- **[DECISION] スマホもタイトルを先頭に、締切をタイトル直下へ**（P29の column-reverse を撤回）。
  「タイトルが主役」の原則を両幅で揃えた。締切の視認性の担保: ①束が締切順で近いものが上
  ②日付は墨太字13.5px ③残り14日以内は朱＋明滅。`.trow` の flex 方向を変えただけ（DOMは1つ）。
- **[DECISION] 残り14日以内の締切はゆっくり明滅させる**（PC・スマホ共通）。
  `opacity 1⇄0.5` を**4秒周期（片道約2秒）の ease-in-out**で。朱の色は変えず濃度だけ。
  **`prefers-reduced-motion: reduce` では静止**（ティッカーと同じ扱い）。
  ⚠️**P20の点滅不採用とは事情が違う**: 当時は「重要度の高は緊急性とずれる・毎朝6件が点滅する」
  が理由だった。締切は本当に期限があり、14日以内は常時数件＝意味と表現が一致する。
  ⚠️適用は**助成面の `.g .when.soon` のみ**。紙面の「期限が近いもの」欄には適用しない
  （無影響を機械確認済み）。
- ★ 検証（2026-08-25）: 明滅は実測で opacity 0.99→0.53→0.99 と約2秒ずつ滑らかに変化・
  通常の締切には付かない・reduced-motion の静止規則をCSSOMで確認。
  PC1280／375／320 × 文字3段階 × アコーディオン開＋注意書き開＋条件展開で
  横スクロールなし・86px不変。絞り込み・共有・条件・締切順・紙面側（24項目・明滅なし）の回帰なし。
- ★ 教訓（環境）: **バックグラウンドのタブではCSSアニメーションが停止し、
  opacity が 1.00 のまま計測される**。明滅が「効いていない」ように見えたら、
  まずタブを前面化してから測る（実装の不合格と混同しない・鉄則4）。
- mock-p29.html を削除した（P29本体の実機確認後に削除する、というユーザー指示に従った。
  P30の指摘は実機で本体を見た上でのものなので条件成立と判断）。

## 2026-08-25（P31・助成面の再調整。**3と5は承認待ちで停止中**）

- 実装済み（1・2・4）／**3（絞り込みの選択ボックス）と5（タブの独立ボタン化）はモック承認待ち**
  （`mock-p31.html`・承認後に本実装して削除する）。
- **[DECISION] 明滅は2秒周期・濃度1⇄0.25に**（P30の4秒・1⇄0.5から。ユーザー指示の
  「2倍の速さ・2倍の濃さ」）。reduced-motion の静止は維持。
- **[DECISION] 注意書きはサイド欄（スマホはメニュー内）の「記号の見かた」の隣に畳んで置く**
  （P30の「絞り込みと項目の間に畳む」は不採用・ユーザー判断）。凡例と注意書きは同じ性質
  （読み方の説明）のため。`#boxGrantNote`＝助成面のときだけ表示。
  各項目の「応募の条件」展開内の注記は維持（そちらが実際に読まれる場所）。
- **[DECISION] 社会福祉法人トグルは既定オン**（使用者は基本的に社会福祉法人のため）。
  **保存値がある人はそれを尊重**——`APPLI_KEY` はトグルを実際に触ったときにしか書かれないので、
  保存値＝本人の明示的な選択。無い人だけ既定オン（`?? "1"` の1行）。
- **[DECISION] トグルの意味を「記載なしは表示」に変えた（重要な仕様修正）。**
  ⚠️従来の実装は**記載なし（applicants空）もトグルで落としていた**——P31の指示にあった
  「現在の仕様（記載なしは表示）」という認識と**逆**だった。既定オン化とセットで、
  **落とすのは「応募主体が読み取れていて社会福祉法人を含まない」ものだけ**に修正。
  理由: ①空＝「読み取れなかった」であって「応募できない」ではない（P24の意味づけ）
  ②旧仕様のまま既定オンにすると10件中3件しか出ず、「対象なのに見えない」がP28で警告した
  実害そのものになる。実測: 既定オンで**8件／全10件**（隠れるのは認定NPO・アジア生協の
  「社福を含まない明記あり」2件だけ。小児医療など記載なし5件は表示）。
- ★ 既定オンでは件数が常に「8件 ／ 全10件」の絞り込み表示になり、スマホの見出しにも
  「絞り込み中: 社福のみ」が常時出る。**うるさければ3の本実装で「既定状態は絞り込み扱いに
  しない」調整も可能**（トグルの見た目の整えも3に合わせて行う予定）。
- ★ 検証（1・2・4）: 既定オン8件・保存値"0"の尊重・記載なし表示・注意書きの位置
  （PC=サイド欄／スマホ=メニュー内・記号の見かたの直後・既定閉・紙面では非表示）・
  明滅2s を機械確認。PC1280/375 × 文字3段階で横スクロールなし・86px不変・紙面側回帰なし。

---

# ロードマップ

> **P0〜P25 の行は `fukushi-watch-archive-2026-08.md` の「ロードマップ P0〜P20」「同 P21〜P23」
> 「同 P24〜P26」へ原文のまま移設した**（文字数の維持規則による）。
> **どのフェーズで何をしたかを辿るときはそちらを見る。**

| フェーズ | 内容 | 完了条件 | 状態 |
|---|---|---|---|
| **P27** | 公営競技系・全国対象の助成。馬主財団を助成の源に追加（`umanushi-rss`・締切不明の束）＋日本財団・JKA・NARの原本確認（記録のみ・源の追加なし） | 実データで raw10→1件・タグ付与と助成面の表示/絞り込みを機械確認。3団体の伝聞を原本確認し sources.md に記録 | ✅ 完了（2026-08-25） |
| **P28** | 応募条件の抽出と表示（AIは選ぶだけ・表示は原文の逐語引用・実在の機械照合・9軸・新規のみ）＋fetchHtmlの文字コード対応＋共有ツールチップの横スクロール修正 | 実データ10/10件で抽出・引用照合全通過・pickFunderLink 8/8・PC/スマホ×文字3段階×展開閉で回帰なし | ✅ 完了（2026-08-25） |
| **P29** | 助成面の密度とタブ（タブ案A・PC項目はタイトル主役＋締切右・タグ案1の薄墨テキスト・用途チップPC1行・スマホ絞り込みアコーディオン） | 3幅×文字3段階×開閉で横スクロールなし・86px不変・絞り込み/共有/条件/締切順/紙面側の回帰なし | ✅ 完了（2026-08-25） |
| **P30** | 助成面の視認性3点＋明滅（絞り込み見出しの地色分離・注意書きの畳み・スマホもタイトル先頭・残り14日の締切をゆっくり明滅（reduced-motionで静止）） | 明滅の実測・静止規則の確認・3幅×文字3段階で回帰なし・紙面側無影響 | ✅ 完了（2026-08-25） |
| **P31** | 助成面の再調整5点（明滅2倍・注意書きをサイド欄へ・絞り込みの選択ボックス化・社福トグル既定オン＋記載なし表示への仕様修正・タブの独立ボタン化） | 1・2・4は実装・検証済み。**3・5は mock-p31.html の承認待ち** | ⏸ 途中（2026-08-25・承認待ち） |

### P6 の設計方針の候補（P6の区切り①で検討する。今は実装しない）
- **分野は「絞り込み」ではなく「重要度の重み付け」に使う。**
  選んだ分野外を除外すると、報酬改定・虐待防止など**全分野に効く通知を取りこぼす**ため、
  関係の薄いものは低に落として残す。
- **設定の置き場所**（リポジトリ内のJSON ／ P4のダッシュボードにUIを置く）は、
  **P4 の設計と合わせて決める**。
- **意義**: 判定の前提が可変になれば、高齢分野の法人や**県協会の会員施設にもそのまま配れる
  ツール**になる（台帳冒頭の「昇格前提」と直結する布石）。

---

# 経緯アーカイブ

`fukushi-watch-archive-2026-08.md` にある（P0〜P24-4・P24-3の記録・ロードマップP0〜P26の行。原文のまま保存）。
記録が10件を超えたら、最も古い1件を同ファイルへ原文のまま移してから追記する（CLAUDE.mdの維持規則）。
