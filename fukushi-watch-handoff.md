# 福祉情報ウォッチ — 引き継ぎ台帳（handoff）

> 引き継ぎの入口はこのファイル1本。新しいチャットは、まずここを読んでから作業を始める。
> 二層運用: ①現在地サマリ（毎回読む）②経緯アーカイブ（必要なときだけ読む）。

---

# 現在地サマリ

## この案件は何か

行政（こども家庭庁・厚労省・自治体等）の福祉関連情報を**毎日巡回して更新を検知し、
要約と重要度判定を付けて毎朝メールで届ける**監視ボット。
**巡回 → 差分検知 → 要約 → 重要度判定 → 通知**の5役が連携するマルチエージェント構成。
目的は①実用ツール（毎朝メールで更新を受け取る）②マルチエージェント設計の学習、の2本立て。

## 現在地

- **P22まで完了（2026-08-25）。実運用中＋一般公開中。** 毎朝7時にGitHub Actionsが
  巡回→差分検知→判定（要約・重要度・分野タグ・対応の目安）→メール送信→履歴蓄積→
  締切抽出→状態コミットを自動実行する。
- 巡回しているのは**6口**: こども家庭庁・厚労省・WAM NET・福岡県×2（行政5）＋介護ニュースJoint（報道）。
  **全国社会福祉協議会（団体・P21で実装済み）は「保留（届出前）」で巡回対象外**——
  届出の投函をもって運用開始とする決定のため。**投函後に状態を「巡回中」へ戻すだけで有効化**できる。
  **福祉新聞も照会待ちで保留**（同じ手順で復帰）。
  団体は**行政と同じ扱い**（要約・重要度・分野タグ・締切抽出・件数・グラフに合流）で、
  区別は印章［団］だけ。全国保育士会は調査のうえ**不採用**、全国保育協議会は**保留**。
- 公開ダッシュボード **https://fukushi-watch.fknd.jp/**（新聞紙面構成。
  ⚠️2026-08-27朝時点: CNAMEは復活済み（fbc5a49）で**新ドメインで配信中。ただし http のみ**
  ——GitHubの検証が InvalidDNSError のままで証明書が未発行のため。github.io のURLは新ドメインへ転送される。
  **HTTPSが立つまでCNAMEに触らない**——Remove→再入力のたびに検証と証明書発行が振り出しに戻る）:
  題字＋欄外（和暦・創刊から第N号／創刊前は「創刊前の記録」）・PC2カラム／スマホ1カラム＋
  固定ヘッダーとドロワー・トップ記事＋高中低の濃淡・低は索引・報道欄・検索・分野切替・
  「この7日」（対応が要る日の地図）・「期限が近いもの」・全項目に共有（Web Share／URLコピー）・
  **報道ティッカー**（題字直下・その日の報道を流す・0件の日は非表示）。
  重要度と分野のチップは**PC・スマホとも紙面側**（押した結果が目の前で変わる位置）。
- **助成で稼働しているのは5口**（JFC・中央共同募金会・福岡県共同募金会・WAM助成・
  中央競馬馬主社会福祉財団=P27）。馬主財団は締切が構造的に取れない源で、**「締切不明」の束に入る
  （それでよい・P27の[DECISION]）**。
  ⚠️**全社協ハブは「保留（届出前）」で停止中**——届出が未投函のため（2026-08-25に是正）。
  **jyosei-navi も「保留（照会前）」**で実装済み・未稼働（承諾後に1行で有効化）。
- **助成面（P24）**: 題字下のタブで紙面と切り替え。`data/grants.json`（いま応募できる助成）を
  締切あり／随時・通年／締切不明の3束で表示。分野（単一）×用途9値（複数OR）×応募主体トグルで絞る。
  共有は `?v=grants&g=hash8`。**紙面からの導線は「期限が近いもの」欄の下に1本**。
  絞り込みは **分野（単一）× 用途（単一・P25で複数選択から変更）× 応募主体トグル**、いずれもAND。
  用途に「すべて」のチップは無く、**同じチップをもう一度押すと解除**（選択中は「×」が出る）。
  各項目に**「応募の条件」のタップ展開**（P28）: 募集ページからの**逐語引用**を9軸で並べる。
  AIは「どこが条件か」を選ぶだけで表示は原文（判定はしない・できない構造）。
  空＝「条件の記載を確認できませんでした」の正直表示＋必ず募集要項への導線。
- **研修面（P34・第3のタブ）**: `data/kenshu.json`＝助成と同じ別系統。第1弾は
  **福祉・介護研修センター（県社協の専用サイト）の年間予定表**のみ・**AI不使用の機械抽出**
  （締切・分野・種別2値=法定・資格系/任意・スキル系）。56件（受付中7・締切不明49。介護福祉士会26件含む）で稼働中。
  分野は**タイトル語彙→（法令対応は共通のまま）→源の既定分野**の3段（P36。共通22・高齢31・障害3）。
  ⚠️年間予定表のURLは**毎年4月に更新が要る**（sources.md #18・変え忘れは毎朝のエラーで気づける）。
  合流・新着も稼働済み。ケアマネ法定研修だけは募集期に別設計（P34の記録を見よ）。
- 履歴は**40日分・633件（高71）**（2026-07-01〜）。P14で7/1〜8/15の547件を遡及記録し、
  **関連記録の系列照合が効いている（related 117件）**。
- 分野タグは**機械的に正規化**（4分野すべて／共通と個別の混在 → `["共通"]`。P15）。
  **P36で「源の既定分野」を追加**——主催者・掲載元そのものが分野を持つ源
  （介護福祉士会=高齢・Joint=高齢 等・正本は sources.md の「既定分野」列）では、
  「共通」に落ちた項目を既定分野へ倒す。全分野向けの内容（虐待防止・BCP・感染症・
  外国人材等）は共通のまま。3面すべての経路（summarize/backfill/normalize-fields/kenshu）に適用。
- 欄外は**常に本日**の日付と号数を示す帯（P22。閲覧中の紙面の日付は日付見出しに出す）。
  **過去を見ているときだけ欄外の日付が「本日へ戻る」ボタンになる**（本日なら押せない）。
  スマホは中間形「令和8年8月25日（火）」・PCは「2026年（令和8年）8月25日　火曜日」。
- 件数表示は**PCが内訳あり／スマホは「新着N件」のみ**（P22）。分野で絞り込み中の表示は両方で維持。
- 原本への導線は**見出しリンクに一本化**（P22。「原本を確認する →」は廃止）。
- スマホは**文字サイズを3段階**（標準/大/特大・メニュー内・localStorage記憶）。拡大は本文系のみ。
- 運用コストは完全ゼロ（Gemini無料枠・Actions無料枠）を維持。

## 次の一手

- ⚠️ **対外4件（JFC・中央福祉学院・WAM NET・全社協）＋福祉新聞の送付は、Enforce HTTPS を
  有効にしてから。** 文面のURLは全て https://fukushi-watch.fknd.jp/ に統一済みで、
  証明書発行前に送ると受け手のブラウザに**セキュリティ警告**が出る（相手は行政・全国団体・専門紙）。
  待ちは時間単位のため、待って出す。
- ★ **課題: 現在地サマリが肥大している**（「生きている注意事項」だけで9千字超・38項目）。
  二層運用の「1〜2画面以内」を大きく超えており、**記録の移設では解けない**
  （移設は下段＝決定事項に効く。上段は別の整理が要る）。対外5件の送付が片付いたあとに
  整理の作業を立てる。⚠️移設の凍結は解除した（2026-08-27）——「完了済みのうち最古」の
  規則が同じ役割（生きている前提を残す）を果たすため、二重の歯止めは不要と判断。
- **WAM NETへの照会メール送付（ユーザー作業・P41）。** 文面は
  `~/Documents/fukushi-watch-taigai/wamnet-link-shokai-2026-08-26.html`（氏名・連絡先を差し替えて
  webmaster2@wamnet.wam.go.jp へ送信）。**承諾が得られたら区切り②（件名索引 kenmei.js）を実装**。
  差し支えの回答なら件名索引は取りやめ（リンク箱のみで完了）。
- **助成財団センターへの照会送付（ユーザー作業）。** 文面は
  `~/Documents/fukushi-watch-taigai/jfc-navi-api-shokai-2026-08-25.html`。
  **推奨はウェブフォーム**（`https://www.jfc.or.jp/contact/` の「助成団体ポータルサイト“助成・奨学金情報navi”」）。
  **承諾を得たら** `docs/sources.md` の #11 を「巡回中」に1行変えるだけで有効化できる。
- **助成の観察**（1〜2週間の実運用で見る）:
  ①**WAM助成が募集期に0件から立ち上がるか**（年1〜2回。0件が続くのは正常だが、
  募集が始まっても0件のままなら見出しの絞り込み条件が狭すぎる）
  ②**共同募金会の締切表記の揺れ**（【…M/D締切】【…M/D必着】以外の書き方が出ないか）
  ③**重複統合の誤爆がないか**（件数が静かに減るだけなので気づきにくい。
  同じ財団の別助成が消えていないかを目で見る）
  ④**JFCの福祉外除外の精度**（毎回2件前後が落ちている。福祉の助成まで落ちていないか）
- **全社協への届出の投函（ユーザー作業）。** 投函後に `docs/sources.md` の
  **#9と#10の2行とも**「保留（届出前）」→「**巡回中**」へ変更してコミット・プッシュすれば運用開始。
  ⚠️**必ず2行そろえる**（届出は全社協サイトへのリンク設置についてのもので、
  新着情報も助成情報も同じ対象。片方だけ動かすと届出前の運用になる）。
  ⚠️**あわせて `index.html` の表示も戻す**（P32で実態に合わせて変えてある）:
  「このサイトについて」の表の全社協の行「準備中」→「毎朝7時」＋題字の副題に「・全社協」を戻す。
  投函後に `node scripts/grants.js` を1回走らせれば助成7件が戻る。文面は
  `~/Documents/fukushi-watch-taigai/zenshakyo-link-todoke-2026-08-21.html`（ブラウザで開いて印刷）。
  **差出人（住所・氏名・連絡先）と投函日は差し替えが要る。** 返事は待たない運用のため、
  投函後にすることは無い（`sources.md` は既に「巡回中」）。
- **P21の初回巡回の見届け。** 初回はベースライン保存のみで新着0件（設計どおり）。
  **団体の項目が紙面に出るのは2日目以降**。翌朝のActionsで［団］印章と判定を実データで確認する。
- **全社協の過去分のbackfill（投函後に実行）。** 改修は済み・下見で9件を確認済み。実行は:
  `node scripts/backfill.js --from 2026-07-01 --to 2026-08-20 --ignore-progress`
  （6日分・9件・6リクエスト。⚠️`--ignore-progress` を外すと5日分が抜ける）。
  実行後は `node scripts/relink.js` で関連記録を付け直す（P14と同じ手順）。
- **福祉新聞への照会送付（ユーザー作業）→ 回答を待つ。** 文面は
  `~/Documents/fukushi-watch-taigai/fukushishimbun-shokai-2026-08-27.html`（2026-08-27作成。
  **送付経路の調査結果: 一般問い合わせのフォーム・公開メールは無く、会社概要に
  「その他のお問い合わせは、お電話にて」（TEL 03-3581-0431／FAX 03-3581-0433）。
  文書照会は郵送を推奨**——宛先住所は文面に記載済み。無断転載禁止の明記を踏まえた
  「可否を伺う照会」の形・承諾まで掲載しない）。
  掲載可なら `docs/sources.md` の福祉新聞行を「巡回中」へ戻すだけ（コード変更不要・
  stateのベースラインも温存済み）。不可なら「掲載不可」に更新して終了。
- 上記以外は指示書待ち（停止中）。
- 観察ポイント（1〜2週間の実運用で見る）:
  ①メール分量 ②「高」インフレ（P6の中立化で改善傾向）③分野タグの精度（特に「共通」）
  ④対応の目安（action）の距離感と品質 ⑤締切ウォッチが対象のある朝に回るか。


## 生きている注意事項

- ★ **UIのラベル・文言は最小限にする（P31・ユーザー指示「今後も配慮する」）。**
  文脈から予想できる語は書かない（実例: 「社会福祉法人が応募できるものだけ」→「社会福祉法人」）。
  新しいUIを作るときは、まず削れる語がないかを確認してから出す。
  ⚠️**この原則が削るのは「予想できる語」だけ**（P45で調停・下記の[DECISION]）。
  「記載なし」のように**省くと意味が反転する語は削らない**。判定は「この語を消したとき、
  読み手が正しい既定値を補えるか」で行う——補えるなら削る・補えないなら残す。
- ★★ **特定ページの中身を監視する型（page-watch）の設計原則**（P35・ユーザー承認）:
  ① ページ全体のハッシュを使わない。「ページの変化」ではなく
  **「監視の意図に合う要素の集合の変化」**を見る
  ② 誤検知防止は3層（領域の限定・採用/除外語彙・要素キーの設計）
  ③ ⚠️**最重要は要素キーの設計。アンカーテキストを主・URLを従にする。**
  これにより「URLが年度替わりで差し替わる様式PDF」は発火せず、
  「受付中の文言が新出する」は発火する。片方を潰すともう片方が漏れる構造を
  キーの設計ひとつで解決できる
  ④ 受付期外で実データの発火を確認できない場合は**合成テストで両方向を担保**する
  （注入した変化で発火すること／無関係な変更で発火しないこと）。
  実装: `kenshu.js` の `watchElements`＋`WATCH_RULES`（将来の適用先はルール1エントリ＋
  台帳1行で足せる。全国保育士会の処遇改善ページ・福岡県社協の民間助成ハブが候補）。
- ★ **源の採否を判断するときは、一覧のタイトルだけでなく記事本文の実体まで確認する**
  （P35・ユーザー指示）。タイトルに目的の語が流れていても、本文が誘導文だけで実体がPDFに
  あることがある。P35の実装順序2（福岡県介護支援専門員協会）はこれで見送りになった。
- ★★ **サイトの見出し・セクション名を、そのまま分類として写さない**（P34-2の教訓・ユーザー指示）。
  **見出しは「並べ方」であって「性質」ではないことがある。**
  実例: 福祉・介護研修センターの「階層別研修」セクションは、新任→中堅→管理職の順に
  **その階層向けのテーマ研修（コーチング・リスクマネジメント等）まで含めて並べた並べ方**であり、
  研修の性質ではなかった。そのまま写すと**14件が階層別**になった（正しくは2件）。
  ⚠️**提案時の写像と実装後の数字を照合したことで発見できた。
  分類を実装したら、必ず件数を事前の想定と突き合わせること。**
- ★ **研修の源の選定方針（P34・ユーザー指示）: 情報源は多いほど良い。網羅性を最大化する。
  ただし民間の小規模な研修・セミナーは除外する。**
  ⚠️内容での判定は困難（同じ「リスクマネジメント研修」でも県社協主催と民間業者では扱いが違う）。
  **主催者の性質で切ることを原則とする**——
  載せる: 法定研修・資格系／行政および受託実施機関／職能団体・業界団体／社協・公的法人。
  除外: 営利目的のセミナー／集客が主目的の説明会／商品・サービス紹介を兼ねたもの／個人主催。
  ⚠️主催者投稿制の源（WAM NETのセミナー情報等）を採用する場合は主催者の判定が必須になる。
  **源の選定段階で公的なものだけを選べば判定が不要になるため、そちらを優先する。**
- ★ **承認の範囲を超える拡張は、小さくても実装前に確認する**（P36-2・ユーザー指示・2026-08-26）。
  実例: P36で調査報告に無かった「子ども・青少年=児童」を実装後に追認を求めた。今回は妥当と
  追認されたが、**原則は報告→承認→実装の順序**。承認済みの原則の適用範囲内に見えても、
  対象を新しく増やす判断は先に出す。
- ★ **指示側が仕様を誤認していた場合、実装前に実データで確認して食い違いを報告すること**
  （P31・ユーザー指示）。実例: 「現在の仕様（記載なしは表示）」という前提が実際と逆で、
  そのまま実装すれば対象の助成が見えなくなるところだった（既定オンで10件中3件になる）。
  指示に書かれた「現在の仕様」も鵜呑みにせず、コードと実データで確かめてから動く。
- ★ **鍵類の値を、リポジトリ・台帳・チャットに絶対に書かない。** Gemini API キーと
  Gmail アプリパスワードは **GitHub Secrets にのみ**保管する。文書に書いてよいのはシークレット名まで。
- ★ **運用コストは完全ゼロを維持する**（Gemini API 無料枠・GitHub Actions 無料枠）。
  課金が発生する選択肢は、採用前に必ずユーザーに確認する。
- ★ **差分スナップショットに監視先ページの全文をコピーして保存しない。**
  将来にわたって、保存するのは**ハッシュ＋見出し等の最小限**にとどめる。
  理由: ①官公庁ページの全文保存は著作権上の配慮が要る ②全文を貯めるとリポジトリが肥大し、
  差分の確認そのものが重くなる ③変更の検知に必要なのは「変わったか」と「どこが変わったか」であり、
  本文の複製ではない。**原本への直リンクを常に残し、最終判断は原本確認に誘導する**（dev-workflow 鉄則5）。
- ★ **外部の鍵・アカウントが要るフェーズは、指示書の冒頭に「手順0」を置く**
  （設定の雛形を作る → 入手先を案内する → いったん停止して待つ）。ユーザーに準備を先回りさせない。
- ★ **依存ゼロ構成を維持する**（npm パッケージを足さない。HTML解析も正規表現・SMTPも自作）。
  Actions で `npm install` が不要になり、壊れる面が少ない。複雑なHTMLの源が増えたら再検討可。
  ※アーカイブへ移したP1の決定だが、いまも全スクリプトを縛る制約のため昇格。
- ★ **抽出0件は「差分なし」でなく構造変化の疑いとして失敗扱いにする**（サイレント0件の禁止）。
  源ごとに失敗を隔離し、全滅のときだけエラー終了する。※同上（P1）。
- ★ **AIのモデル名を固定で書かない**（一覧取得→新しい順に試行→成功モデルを記録して次回優先）。
  **応答は件数一致・語彙・形式を必ず機械検証**し、落ちたら次のモデルで再試行、全滅なら失敗終了。
  ※同上（P2。世代交代404と件数抜けへの構造的対策）。
- ★ **項目ハッシュは「タイトル＋日付＋URL」**（URLだけにしない。同一URLへの繰り返し掲載がある）。
  ※同上（P1）。
- ★ **daily.yml の順序は 巡回→判定→送信→履歴→締切→状態コミット。状態コミットを送信より後に置く。**
  送信に失敗した日は状態が更新されず、翌朝に同じ新着を再検知して送り直せる（取りこぼさない構造）。
  ※アーカイブへ移したP3の決定だが、順序を変えるとこの保証が壊れるため昇格。
- ★ **Actions の cron は指定時刻ちょうどには動かない**（数分〜十数分の遅延が正常）。
  「7時を過ぎたのに来ない」はまず遅延を疑い、Actions の実行履歴を見る。※同上（P3）。
- ★ **運用の鍵の落とし穴（再発しうる）**: Gmail SMTP は**そのGoogleアカウントにGmailアドレスが
  存在すること**が必要（外部アドレスのログインIDでは 535 になる）。`.github/workflows/` を push する
  トークンには **`workflow` スコープ**が要る（`repo` だけでは拒否）。※同上（P3）。
- ★ **ファイルの追記は、読み込みと書き込みを1つの式にまとめない。**
  Pythonの引数評価順などにより、**読む前に書き込みモードでファイルが開かれて中身が消える**
  （実例: `open(p,'w').write(open(p).read()+追記)` でアーカイブが空になった）。
  **読み切って閉じてから、改めて書く**手順にする。リポジトリ管理下なら git から復元できるが、
  **`data/` 配下の未コミットファイルは復元できない**（state.json・履歴を扱うときは特に注意）。
- ★ **ローカル検証と Actions の自動コミットは衝突する**（data/ の差分）。ローカルの未コミット
  data/ は `git checkout -- data/` で捨て、push 拒否は `git pull --rebase` で取り込む。
  **Actions 側が常に正**（そちらが新しいため）。※アーカイブへ移したP5の教訓だが再発頻度が高く昇格。
- ★ **判定の骨格（P6で確定・いまも全機能の前提）**: 分野タグの語彙は**保育/障害/高齢/児童/共通**の
  5値＋空配列（＝福祉と無関係）。**迷ったら空でなく共通に倒す**。重要度は**分野中立で1つ**だけ付け、
  受け手ごとの調整は分野タグで行う（メールは `data/mail-settings.json` の重み付け、
  ダッシュボードは動的な絞り込み）。**25件超は複数リクエストに分割**する。
  読み手は **fields の欠落を空配列として扱う**。※アーカイブへ移したP6の決定だが全機能の土台のため昇格。
- ★★ **分野タグの判断の原則（P37-1・3面共通・ユーザー承認済み）**
  語彙は5値＋空配列（P6）。「共通」は「全分野に効く」という強い意味で、分野で絞っても常に
  表示される＝どの分野の職員にも見える。空配列は「福祉と無関係」＝表示から消える。
  **判定の優先順と、その順序の理由**:
  ① **個別判定**（タイトル語彙・AIの判定）——内容そのものが分野を語るなら、それが最も確かな材料。
  ② **全分野向けの例外**（→共通のまま）——内容が制度として全分野に効くなら、どんな場に
  載っていても共通が正しい。研修面は種別=法令対応で、紙面・報道は語彙（ZENBUNYA_KW）で判定。
  ③ **源の既定分野**——内容から分野が読めないときに初めて「誰に向けた場か」で補う。
  **内容の証拠＞場の証拠**の順序。逆にすると個別の証拠を場の先入観で潰す。
  ④ **共通**——P6の「迷ったら空でなく共通」は**今も有効**。誤りの向きとして「余計に見える」は
  「見えない」より安全。**理由: 見えない誤りは読み手が気づけないが、余計に見える誤りは
  読み手が自分で捨てられる**——これは分野タグに限らず本サイト全体の設計思想であり、
  助成の応募主体タグの「記載なしは表示する」（P31）も、条件展開の「読み取れなければ
  正直に出す」（P28）も、同じ思想に基づく判断である。
  **「分野をまたぐ制度」を例外（②）にする基準**: **制度・義務として複数分野の事業所が対象に
  なるもの**（虐待防止・BCP・感染症・身体拘束・権利擁護・外国人材の在留制度・喀痰吸引等研修）。
  ⚠️「結果的に他分野でも役立つ」汎用スキル（接遇・コーチング等）は例外にしない——
  対象を決めるのは内容でなく開催の場なので、既定分野へ倒すのが正しい。
  語彙は実データで必要になったものだけ足す（机上で増やさない）。
  **源に既定分野を与える基準**: **その源の受講者・読者が特定分野に事実上限られるか**
  （会員組織=介護福祉士会・分野別の行政ページ・分野専門メディア=Joint）。全分野を扱う機関
  （研修センター・中央福祉学院・厚労省・共同募金会）には与えない。**新しい源を足すときの問い**:
  「この源の項目を、その分野以外の職員が利用することが普通にあるか」——あるなら空欄。
  **迷ったら空欄**（共通に落ちる＝見える側に倒れる）。
  **運用中に分野が不適切に見えたときの直し方（この順で疑う）**:
  ①個別判定の読み違い → 語彙・プロンプトの修正 ②内容が分野をまたぐ制度 → 例外語彙に足す
  （実データ確認が先） ③同じ源が繰り返し同じ方向にずれる → 既定分野の見直し（台帳の1セル）。
  ⚠️いずれも「実データで確認 → 原則に照らす → 最小の1手」。**1件のために構えを変えない。**
- ⚠️ **jyosei-navi は網羅のベースではない。** 検索APIは**募集期間が登録された現役の公募のみ**を返し、
  **主要な企業財団が検索に出ない**。P24-3で保存した福祉110件を再検証した実測（2026-08-25・
  新規リクエストなし）:
  **出てこない** → 洲崎福祉財団・大和証券財団・SOMPO福祉財団・松翁会・みずほ教育福祉財団・
  日本生命財団・キリン福祉財団・三菱財団・中央共同募金会
  **出てくる** → 日本フィランソロピック財団・齋藤茂昭記念財団・松の花基金
  ⇒ **JFC新着RSS＝フロー／jyosei-navi＝ストック の併用が前提**で、
  **naviはRSSの上位互換ではない**。「naviに主要どころが入っている」は誤り。
  ⚠️ **全社協ハブ（`/guide/sponsor/`）が企業財団系の穴埋めそのもの**であり、
  **届出の投函が網羅性の最優先事項**である（上に挙げた9つのうち大半は全社協ハブに載る）。
- ⚠️ **jyosei-navi API へは、助成財団センターの承諾を得るまでリクエストしない**（ユーザー指示・2026-08-25）。
  照会文面に「毎朝1回・最大21リクエスト」と書いており、承諾前の追加アクセスは約束と食い違う。
  検証が要るときは **P24-3で保存した110件のスナップショット**を使う。
- ⚠️ **backfillで積んだ項目には締切ウォッチ（P11）が走らない。**
  遡及分は**掲載日の紙面**に入るため、締切のある項目が含まれていても
  **「期限が近いもの」欄には出ない**（`deadlines.js` の入力は当日の `report-latest.json` のみ）。
  P14で「遡及分に締切抽出はしない」と決めたのは**期限切れが大半という前提**だったが、
  **P26では遡及範囲に有効な締切が含まれた**（介護ＤＸ支援事業費補助金・8/31締切を8/3の紙面に記録）。
  将来、**遡及分にも締切抽出を通すかを再検討する余地がある**。
- ★ **年が省略された日付表記（M/D）は、掲載日を基準に解く。**
  「今日以降でいちばん近い年」で解くと、更新されない古い記事の締切が翌年に繰り上がり、
  **締切済みのものが未来の予定として並ぶ**。しかも未来日なので**期限切れ削除にも引っかからず、
  静かに間違ったデータが残り続ける**（P24-4で福岡県共同募金会の締切済み2件が2027年として出た）。
  掲載日基準で解けば過去日になり、正しく落ちる。
- ★ **重複の統合は、条件を緩くすると別物まで畳む。**
  **同一の財団が同じ締切日で複数の助成を出すことは実際にある**（SOMPO福祉財団の「組織強化」と
  「認定NPO取得」はどちらも締切2026-10-09）。統合の判定には必ず
  **助成そのものを特定できる条件（締切の一致＋名称の包含関係）**を使い、
  **出し手名だけでの照合は「片方が空のとき」に限る**。緩い照合は件数が減るだけで気づきにくい。
- ★ **NFKC正規化を通したテキストに、全角のままの正規表現を書かない。**
  `normalize("NFKC")` は `（）`→`()`、`～`(U+FF5E)→`~`(U+7E) に変える。全角で書くと一致せず、
  **黙って全件が「取れなかった」になる**（P24-4でJFCの締切が全件unknownになった）。
  波線は U+7E / U+FF5E / U+301C の3つを許すこと。
- ★★ **源の性質や状態を、コードに埋め込まない。必ず台帳（`docs/sources.md`）を参照する。**
  実例3件:
  ① `backfill.js` が**報道源を名前で列挙**していた（P24-4）
     → 報道源を1つ足すとこのスクリプトだけ取り残され、**報道にAI要約を付ける**ところだった
  ② `backfill.js` が**全社協を無条件に取得**していた（P26）
     → 届出前で「保留」にした源を、**遡及だけが取りに行く**ところだった
  ③ `backfill.js` が **P15の正規化を持っていなかった**（P21）
     → 生の `fields` を書き、遡及分だけ正規化から漏れるところだった
  ⚠️ **いずれも `backfill.js` で起きている。** 本流（`crawl.js` / `summarize.js`）に入った判断が
  単発スクリプトに届かない構造が繰り返し露見している。
  ⚠️ **「止める」という判断は、毎朝の巡回だけでなく、遡及・再計算・正規化のすべての経路に
  等しく効かなければならない。** 新しい単発スクリプトを作るときは、
  **本流が持っている判断（台帳の状態・区分・正規化）をすべて通しているか**を確認すること。
  ★ 点検の実施（2026-08-25）: `relink.js`・`normalize-fields.js`・`deadlines.js`・`grants.js` を確認。
  前3つは**取得を伴わず既に記録済みのデータだけを扱う**ため台帳参照は不要（`deadlines.js` の入力は
  `report-latest.json` で、そこに至る時点で台帳の絞り込みを通っている）。`grants.js` は
  `readSources()` で区分と状態を見ており正しい。**停止中の源のデータ残留も0件**
  （履歴・`deadlines.json`・`grants.json` の全てを機械照合）。
- ★ **団体源(P21)には、他源に無い「原本の禁止文言チェック」が挟まっている**（`checkReprintNotice`）。
  全社協への届出で約束した運用のため、**外してはいけない**。⚠️取得失敗を「除外」にすると
  一時的な通信障害で項目が永久に消える（state に載って二度と再評価されない）。
  失敗は必ず**保留＝stateにも載せず翌朝やり直す**側に倒す。新しい団体源を足すときも同じ。
- ★ **対外文書（届出・照会状）はリポジトリに置かない。** 置き場は `~/Documents/fukushi-watch-taigai/`。
  公開リポジトリに個人の住所・氏名が入る事故を構造的に防ぐ。
- ★ **プレビューサーバは `~/Documents` 配下を読めない**（全て404になる）。実機確認は
  スクラッチパッドへ `index.html` と `data/` を複製して配信する。検証用にデータを細工したら
  **`git checkout -- data/` で必ず復元する**（P16の作法）。
- ★ `docs/sources.md` の**確定済みの行は承認なしに書き換えない**。URLの確定はP1（1源目）・P4（残り4源）。
- ★ 報酬改定関連は **報酬改定ナビ**（`~/Documents/houshu-navi/`・kanda-houtokukai/houshu-navi）と
  情報源が重なる可能性がある。突き合わせて重複監視を避ける。
- ★ **本案件は「個人の試験開発」**。法人PT案件ではないため `references/pt-context.md` は適用しない。
  ただし**形になったら PT・県協会への提示物に昇格**する想定なので、他人が見る前提の作りにしておく。
- ★ **停止規律（P9の教訓・2026-08-18）**: 指示書に「区切り②は区切り①の承認後のみ」とある場合、
  **その区切りの承認を明示的に確認してから次へ進む**。選択肢の提示に推奨を添えて一括承認を得る形は、
  判断を先導して停止ポイントを実質すり抜けることがある。特に**著作権・対外関係など
  実装の妥当性と別軸の判断**が含まれる区切りでは、論点ごとに承認を分けて確認する。
- ★ **メールの分量（P5で実測・承認済み・引き続き観察）**: 5源の実測は**平日12〜18件/日**
  （当初懸念の30〜50件より少ない）。15件のメールをユーザーが確認し「このまま運用」で承認済み。
  それでも長いと感じたら: **高・中はメール本体として現状維持、低は圧縮**（件数の集約のみ等）。
  低の詳細は P4 ダッシュボードの役割とし、**メール＝判断が要るもの／ダッシュボード＝全件**の分担。
- ★ **「高」判定のインフレ傾向（観察中・P4以降で判断）**: 感染症の**定期更新データ**
  （コロナ定点報告の推移など毎週来るもの）まで「高」に判定される。基準の「感染症=高」に忠実な
  結果だが、毎週高が並ぶと件名の「高N件」が薄まる。1〜2週間の実運用で頻度を見て、
  プロンプトに「定期更新の統計データは中」等の微調整を検討する。

## ブロッカー

- なし（福祉新聞の照会待ちのみ。運用は継続中）。

## ファイルの地図

| ファイル | 役割 | 正本か |
|---|---|---|
| `fukushi-watch-handoff.md` | この台帳。引き継ぎの入口 | ✅ 正本 |
| `CLAUDE.md` | Code がセッション開始時に自動で読む案件固有ルール | ✅ 正本 |
| `docs/sources.md` | 監視対象台帳（源の名前／URL／巡回方法＝パーサ名／状態） | ✅ 正本 |
| `scripts/crawl.js` | 巡回＋差分検知（P1）。台帳の「巡回中」行だけを巡回。依存ゼロ | ✅ 正本 |
| `scripts/summarize.js` | Gemini要約＋重要度判定（P2）。diff→report。依存ゼロ | ✅ 正本 |
| `data/state.json` | 前回巡回時の項目リスト＋`preferredModel`（成功したGeminiモデル） | 実行で更新 |
| `data/diff-latest.json` | 直近実行で検出した新規項目。**P2の入力** | 実行で更新 |
| `data/report-latest.json` | 要約＋重要度付きの新規項目。**P3のメール本文の材料** | 実行で更新 |
| `.env.example` | 必要な環境変数の雛形（`GEMINI_API_KEY=`・値は空） | ✅ 正本 |
| `.env` | 鍵の実値。**git追跡外**（.gitignore済み）。値はどこにも書かない | ローカルのみ |
| `scripts/mail-template.js` | 件名とHTML本文の組み立て（P3）。P4ダッシュボードも同じ材料を使う | ✅ 正本 |
| `scripts/notify.js` | Gmail SMTPS(465)送信（P3）。依存ゼロの自作クライアント | ✅ 正本 |
| `.github/workflows/daily.yml` | 毎朝7時(JST)=22:00(UTC)の定期実行＋手動実行 | ✅ 正本 |
| GitHub Secrets | GEMINI_API_KEY / GMAIL_USER / GMAIL_APP_PASSWORD / MAIL_TO の4つ | 値はGitHubのみ |
| `scripts/archive.js` | 履歴蓄積（P4）。report を月次＋索引へ。同日はハッシュ併合 | ✅ 正本 |
| `data/history/` | `YYYY-MM.json`（日毎の全項目）＋`index.json`（日毎件数） | 実行で更新 |
| `data/mail-settings.json` | メールの分野重み付け設定（現在=障害・児童・保育） | ✅ 正本 |
| `index.html` | ダッシュボード本体（依存ゼロ・単一ファイル） | ✅ 正本 |
| https://fukushi-watch.fknd.jp/ （HTTPS発行待ち・現在は http のみ。github.io は新ドメインへ転送） | 公開ダッシュボード（Pages・main/root） | — |
| `scripts/deadlines.js` | 締切抽出（P11）。本文取得→幻覚ガード三重検証→期限のみ保存 | ✅ 正本 |
| `scripts/grants.js` | 助成金の収集（P24・**紙面と別系統**）。区分=助成の源だけを扱う。締切は正規表現で抽出（AI不使用） | ✅ 正本 |
| `~/Documents/fukushi-watch-taigai/` | **対外文書（リポジトリ外）**。全社協あて届出／助成財団センターあて照会 | ✅ 正本 |
| `data/grants.json` | **いま応募できる助成**（締切超過は保存からも削除）。紙面のhistoryには積まない | 実行で更新 |
| `data/deadlines.json` | 追跡中の期限（期限日・種類・根拠文字列・hash） | 実行で更新 |
| `scripts/backfill.js` | 過去分の遡及記録（P14・**手動のみ**）。stateは読むだけ・メール送信なし。P21で全社協に対応（禁止文言チェック込み）＋重複判定を履歴全体に修正 | ✅ 正本 |
| `scripts/relink.js` | 全履歴の関連記録を再計算（P14・**手動のみ**・AI不使用） | ✅ 正本 |
| `data/backfill-progress.json` | バックフィルの処理済みの日（再開用） | 実行で更新 |
| `~/Documents/fukushi-watch-taigai/` | **対外文書（リポジトリ外）**。全社協あて届出の印刷用HTML | ✅ 正本 |
| `fukushi-watch-archive-2026-08.md` | 経緯アーカイブ（P0〜P28（P24の枝番・P25の後を含む）の記録とロードマップP0〜P32・P34の行を原文のまま保存） | 参照用 |
| `package.json` | npm run crawl/summarize/notify/archive の定義のみ（依存なし） | ✅ 正本 |
| `~/Documents/fukushi-watch/` | ローカルの作業場所 | — |
| `github.com/kanda-houtokukai/fukushi-watch` | 公開リポジトリ | — |
| `~/.claude/skills/dev-workflow/` | 作業規範（全案件共通） | ✅ 正本 |


---

# 決定事項（[DECISION]）

> P29の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-26）。

> P30の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-26）。

> P31の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-26）。

> P32の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-26）。

> P34の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-27）。

> P34-2の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-27）。

> P36の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-27）。

> P38の記録は fukushi-watch-archive-2026-08.md へ原文のまま移設した（維持規則・2026-08-27）。

## 2026-08-26（P33・アイコンとカスタムドメイン。**2（ドメイン）はDNS待ちで保留**）

- **アイコン設置（1・完了）**: リポジトリ直下に5ファイル
  （apple-touch-icon 180／icon-192／icon-512／favicon-32／favicon-16）。
  従来のインラインSVG faviconをPNGセットに置き換えた。
  ⚠️**headのパスは相対**にした——github.io のサブパス配信でも、将来のカスタムドメインの
  ルート配信でも同じHTMLが効く（絶対パス `/favicon…` はサブパスで404になる）。
- **[DECISION] manifest は最小構成で作った**（`manifest.webmanifest`）。
  表示名（福祉情報ウォッチ）とホーム画面追加時のアイコン（192/512）をAndroidで正しく
  出すためだけのもの。**`display: "browser"` でアプリ化はしない**（Webサイトのまま。
  standalone にするとブラウザUIが消えて「アプリのふり」になるため避けた）。
- **カスタムドメイン（2・保留）**: `fukushi-watch.fknd.jp` → kanda-houtokukai.github.io。
  実測（2026-08-26朝）: JPレジストリの委任は済み（ns1-4.xserver.jp）だが、
  **Xserverの権威サーバ自体が fknd.jp ゾーンに REFUSED を返す**＝キャッシュの伝播待ちでなく
  **Xserver側でゾーンがまだ有効化されていない**。取得直後の反映待ち（最大24時間程度）の可能性が
  高いが、**丸1日経っても REFUSED のままなら、Xserverサーバーパネルのドメイン設定/DNS設定を
  確認する必要がある**。確認コマンド: `dig @ns1.xserver.jp fukushi-watch.fknd.jp CNAME`
  （REFUSEDでなくCNAMEが返れば有効化済み）。
- ★★ **障害と緊急復旧（2026-08-26朝）**: GitHubのCustom domain設定が **CNAMEファイルを
  コミットした時点で、github.io の全URLが `http://fukushi-watch.fknd.jp/` へ301される**
  状態になっていた。ドメインが名前解決できないため**公開サイト全体が到達不能**
  （配布済みの共有リンク・朝のメールからの導線も含む）。**CNAMEファイルを一時撤去して復旧**
  （コミット e29e2e0・github.io の200と全アイコン配信を確認済み）。
  ⚠️教訓: **GitHubのCustom domain設定は、DNSが生きる前でも即座に301を張る**。
  設定入力はDNSゾーンが有効になってから行うこと（「伝播待ちの間も旧URLで見られる」は誤り）。
- ✅ **DNS有効化と再有効化は完了（2026-08-27）**。CNAMEファイル（fbc5a49）で新ドメイン配信が復活。
  残るは **GitHubの検証（InvalidDNSError）を通して Enforce HTTPS を有効にすること**のみ。
  ★★ **待ちの規律（Chat判断・2026-08-27）: 検証が通るまでCNAMEに触らない。**
  Remove→再入力を繰り返すと、GitHubの検証と証明書要求が毎回リセットされ、
  「いつまでも通らない」状態が自作される。確認はシークレットウィンドウで
  https://fukushi-watch.fknd.jp/ を開くだけ（鍵マークが出れば完了→Enforce HTTPSを有効化）。
  **2026-08-28 06:35 JST（作り直しから24時間）を過ぎても通らない場合に限り**、
  Remove→再入力を1回だけ行い、あわせて Xserver の **DNSSEC** と **CAAレコード** を確認する
  （世界中で解けるのにGitHubだけ落ちる症状は、この2つでも起きうる）。
- **ドメイン有効化後にやること（保留中の作業2の中身）**——①③は2026-08-27に前倒しで完了:
  ✅①旧URL→新URLの横断点検（2026-08-27実施・ユーザー指示）: サイト内にハードコード無し
  （共有リンクは `location.origin` で自動追随を確認・manifestは相対・メールは意図的な
  リポジトリリンクのみ=P32の決定）。旧URLが残るのは台帳の経緯記録（原文保存）だけ。
  ✅③対外文書の旧URL書き換え（2026-08-27実施・ユーザー指示）: 全社協・JFC・学院・WAMの
  4通を https://fukushi-watch.fknd.jp/ へ統一し、**旧URL残存0を機械確認**。
  ⚠️ユーザーが2026-08-27朝にCNAMEを再撤去（70acd78）したため、**送付は再有効化＋
  HTTPS発行（Enforce HTTPS）を待ってから**——死んでいるURLの文書を発送しない。
  ✅②**Actionsの自動コミットはCNAMEに触れないことを確認（2026-08-27・daily.yml の現物で検証）**:
  状態コミットは `git add data/` で **data/ に限定**され、CNAME（リポジトリ直下）は staging に入らない。
  push は force なし・checkout も通常。設定画面からのドメイン入力とActionsが同時に走っても、
  GitHubのCNAMEコミットでActionsのpushが弾かれるだけで、その日の状態が未コミットになる
  ＝**翌朝に同じ新着を再検知して送り直す**（daily.ymlの順序の設計がそのまま効く）。壊れない。

## 2026-08-26（P35・研修の情報源拡張。**実装順序1が完了・2〜4はこれから**）

- **[DECISION] 採用4源と優先順（ユーザー承認）**: ①福岡県介護福祉士会（実装済み）
  ②福岡県介護支援専門員協会（新着テキスト型・研修語彙の二段判定）③中央福祉学院
  （**実装は先行してよいが「保留（届出前）」で運用開始しない**。届出文面は全社協と同型・
  対象URLを学院に変えて別途作成）④社会福祉士会の法定研修ハブ監視
  （「特定ページの中身を監視する型」の初実装。**誤検知の防止を設計に含めること**——
  ページ全体のハッシュでは些細な変更で発火する）。
  **見送り5源**は sources.md の補足に理由つきで記録済み（老施協=SPA・知的障害者福祉協会=
  https不調・県保育協会=ページ実質空・市町村社協=市民向け混在・facsw新着=本文空）。
- **[DECISION] 件数は55〜70件見込みで絞り込みの再設計は不要（ユーザー承認）。
  ⚠️70件を超えたら再検討する。**
- **実装順序1: 福岡県介護福祉士会（kenshu-fkaigo）完了**（2026-08-26）。
  一覧ハブ型（`div.kenshu`単位・`detail_NNN.php`への個別リンク・開催日/会場/費用が平文）。
  **締切の記載が無い源**なので開催日ベース（P34-3の型）＝「締切不明」の束に開催日順で並ぶ。
  親睦系（納涼会・交流会等）・受付終了・調査員募集は除外。
  実測: 31行→26件採用・**研修面は計56件**（受付中7・締切不明49）＝試算どおり。
  種別written: テーマ別36・資格要件12・法令対応5・階層別3（REQUIRE_KWに実習指導者・
  認定介護福祉士を追加）。
- ★ 表示の整理: 「開催予定」の束を**「締切不明」に改名**し、説明を「開催日・時期の順」に
  （f-kaigoの「開催日確定・受付中」が旧名では不正確なため）。開催日を持つ締切不明の項目は
  when欄を空にして「開催」行に一本化（「締切の記載なし」を重ねない）。
  並びは**今日以降の最初の開催日**順（⚠️過去の回を含む項目が先頭に来る不具合を実測で発見・修正）。
- ★ 日付抽出ヘルパを共通化（`datesInText`・新着とf-kaigoで共用）。
- **実装順序4: page-watch（kenshu-watch）実装完了**（2026-08-26）。対象4ページ
  （社会福祉士会の法定研修2・研修センター /kaigoshien/・fukuoka-cmトップ＝台帳#23〜26）。
  状態は `data/pagewatch.json`（正規化キーのみ・全文保存なし）。初回ベースライン保存済み:
  **1／0／0／5要素**（素朴なリンク＋語彙抽出の16／12／51／20件から激減＝3層の効果。
  受付期外の0要素は正常）。**合成テスト6項目すべて期待どおり**——
  発火する: 「【受付中】…申込フォーム」注入・「募集要項」リンク注入／
  発火しない: 様式PDFのURL年度差し替え・求人（news2）の入れ替え・全日付の書き換え／
  異常系: 本文領域消失→失敗扱い（0件と構造変化の切り分け・ユーザー支持）。
  検知した項目は「締切不明」束に「募集情報あり」で載り（周辺に締切があれば date）、
  要素がページから消えたら消える。via=watch で統合の優先度は最低。
- **実装順序3: 中央福祉学院（kenshu-gakuin）実装済み・状態=保留（届出前）**（2026-08-26）。
  「募集中の講座」カテゴリの記事から締切（「第一次締切 9/24」等）・研修日程を機械抽出。
  一時有効化して実測（4件・照合一致）し、**保留に戻してデータもパージ済み**（残留0件を確認）。
  ⚠️**教訓（また実データで発見）: 記事ページ全体から締切を拾うと、サイドバーの
  「カテゴリー別新着情報」に載る他記事のタイトルから別記事の締切を拾う**
  （実測: 4件全部が別記事の「第一次締切 9/24」になった）。本文セグメントに絞って修正。
  ⚠️**規約の重要な発見: 学院の「リンクについて」は「リンクはトップページに設定。
  その他の部分に直接リンクすることはできません」と個別ページへの直リンクを原則不可**
  としている（P26で福岡市を見送ったのと同じ構図）。当サイトは記事ごとの直リンク設計のため、
  届出文面は「トップページへのリンクのご連絡」＋**「募集中の講座の各記事への直リンクの
  可否を伺う」一文**を足した形で作成した:
  `~/Documents/fukushi-watch-taigai/gakuin-link-todoke-2026-08-26.html`。
  **[DECISION] 運用開始の条件は「①投函して回答を待ち、承諾が得られたら記事直リンクで開始」**
  （2026-08-26・ユーザー決定）。理由: トップに丸めると面の中で「この項目だけ目的地に
  飛べない」不揃いが生じP34-2に逆行する／学院の資格系（社会福祉主事・施設長資格認定・
  児童福祉司）は他の源で代替できない／対外手続きは既に3件たまっており4件目として
  並行して待てる。⚠️**承諾が得られなかった場合は見送りとする。トップページに丸める形では
  採用しない**（ユーザー決定）。
- **[DECISION] 実装順序2（福岡県介護支援専門員協会）は見送りに変更**（2026-08-26・ユーザー承認）。
  理由: **新着のタイトルには法定研修の語が流れるが、記事本文は誘導文のみで締切・開催日がゼロ。
  募集要項の実体はPDF。「PDFは解析しない」方針の下では期限管理ができず、
  「〜を更新しました」が期限なく並ぶだけになる**（news1.php・研修/イベントページ=
  カレンダー準備中の空ページ、まで実見して確定）。
  **代替: fukuoka-cm のトップページ（主任研修案内PDFが直貼りされる）を実装順序4の
  「特定ページの中身を監視する型」の対象に含める**（ユーザー承認）。PDFの中は読まないが、
  リンクのタイトル行から研修名は取れる。監視対象は計4ページ
  （社会福祉士会の法定研修2・研修センター /kaigoshien/・fukuoka-cmトップ）。

## 2026-08-26（P37・表示3点＋分野判定の原則。**1（原則の記録）は記録案の承認待ち**）

- **[DECISION] PCの絞り込みは sticky 方式で貼り付ける**（P37-2）。内側スクロール容器
  （overflow-y）にしなかった理由: ①題字・タブが永久に場所を取り「題字は流す」（P37-3）と矛盾
  ②入れ子のスクロールバーが2本出る ③ホイールの捕捉位置で挙動が割れる。
  sticky なら通常スクロールのまま、絞り込みが上端に達した時点から固定され一覧だけが流れる。
  対象は3面（紙面=.chipbar／助成・研修=.gfilters）・**PCのみ**（スマホの固定ヘッダー86px体系は不変）。
  **サイド欄は独立して貼り付く**（align-self:start＋sticky。画面より高い日は欄内スクロール・細バー）。
- **[DECISION] PC題字の余白を詰めた**（P37-3）: padding 34/22→16/12・subの間隔10→6。
  **字の大きさ・字送り・二重罫は据え置き＝格は保つ。固定はしない**（題字は「このサイトが何か」を
  示すもので常時見える必要がない・ユーザー指示）。スマホは別定義で無関係。
- **[DECISION] タブ下の細罫を削除**（P37-4。直下の面見出し太罫と二重）。
  **点検で同種をもう1箇所発見**: 報道ティッカーの下の細罫（同じく日付見出しの太罫と二重）→同時に削除。
  他の候補（欄外の細罫=題字との区切りで正当／.about の表罫／.gsec と項目罫=P32対応済み）は問題なし。
- ★ 検証（2026-08-26・DOM実測）: PC1280でスクロール中 chipbar/gfilters/rail の
  getBoundingClientRect().top=0（貼り付き）・背景は紙色・横スクロールなし。375/320で
  sticky非適用・固定ヘッダー86px不変・横スクロールなし。絞り込み回帰（紙面12→高齢5→12・
  研修の種別56⇄5双方向・チップは再描画後に取り直して検証=P31の教訓）。localStorage後始末済み。
  ⚠️**パネル非表示時のスクリーンショットはプログラムスクロールが反映されない**（P30の
  「バックグラウンドtaブでアニメ停止」と同族のツール制約）。座標はrectの実測を正とした。
- **P37-1（分野判定の原則）はユーザー承認のうえ「生きている注意事項」へ★★で記録済み**
  （2026-08-26。誤りの向きの理由1文——見えない誤りは気づけないが余計に見える誤りは
  読み手が捨てられる——を追記のうえ確定）。

## 2026-08-26（P39・一覧の下端をサイド欄に揃える）

- **[DECISION] 一覧の高さの基準を「文書内位置」→「画面上の位置」に変更**（ユーザー指摘の修正）。
  ★真因: P38の式（画面高−文書内位置）は**ページが少しでもスクロールされた状態
  （ブラウザのスクロール復元・全体スクロール後）で、その分だけ一覧の下端が浮く**。
  サイド欄は sticky＋100vh の**画面基準**なので下端に届き、左右で食い違って見えた。
  修正: `getBoundingClientRect().top`（画面基準・サイド欄と同じ）＋**scrollでも再計測**
  （rAFで間引き・passive）。⚠️**topは0でクランプ**——絞り込みが画面外まで送られたら
  一覧は画面いっぱいで止まる。クランプが無いと「送るほど伸びる」が際限なく続き
  ページ末尾（このサイトについて）に到達できなくなる（maxScroll有限を機械確認）。
- ★ 検証: **DOM実測と見た目の両方**（P38の反省・ユーザー指示）。scrollY=0/150/350の全状態で
  一覧下端=画面下端800を維持・スクリーンショットでも左右の下端一致と余白の消失を目視確認
  （タブを前面化し settle 1秒後に撮影＝P38の教訓の適用）。maxScroll=429で有限・
  スマホ375はheightなし/86px/横スクロールなし・P38の各対応（ティッカー・折り返し・面切替）維持。
- ★ 検証残り物の注意: プログラムでの window.scrollTo 検証後に**サイド欄の内部scrollTopが
  349残っていた**（見た目の食い違いの一因になる）。検証でスクロールを触ったら
  rail.scrollTop=0 まで戻すこと（localStorage後始末と同じ扱い）。

## 2026-08-26（P40・5点の改修。**1・2完了／3・4・5は判断・案の提示で停止中**）

- **1（アイコン差し替え）完了**: ユーザー提供の5ファイル（apple-touch-icon/icon-192/512/
  favicon-32/16）を差し替え・寸法検証済み。**キャッシュ対策は必要と判断し、参照に `?v=2` を付けた**
  （ファビコンはブラウザが専用キャッシュで長く保持し、同名差し替えでは古いまま残るため。
  index.html の4リンク＋manifestの2src。**次回差し替え時は v=3 に上げる**）。
  ⚠️iOSのホーム画面に追加済みのアイコンは自動更新されない（再追加が必要）。
- **2（スクロールバー）完了**: サイド欄に一覧側と同じ `scrollbar-color:var(--rule) transparent`
  ＋webkit系を追加。濃さが揃ったことを計算値で確認。
- **3（日付ナビの1行化＋月移動）完了**（ユーザー承認・2026-08-26実装）:
  旧「← 前の記録｜月セレクト｜次の記録 →」の行を廃止し、日付見出しの1行に統合
  （PC=grid 1fr auto 1fr で **矢印は主列の真ん中**・右端に件数）。前後移動の矢印は
  スマホ用の `#prevDayM/#nextDayM` を両幅共通にした（PCの旧 prevDay/nextDay は削除）。
  **月移動は日付見出し（＋▽）を押すと開くポップオーバー**: その月の記録のある日の一覧
  （日付＋件数・選択中は太字地色）＋月の前後‹›。外側クリック/ESC/日の選択で閉じる。
  ⚠️**PCのみ**（スマホは ▽ 非表示＋pointer-events:none で従来どおり）。
  件数は**両幅とも「新着N件」のみ**（高中低の内訳＝.tally は廃止・P22の分岐を統合）。
  絞り込み中はP19の「障害 3件 ／ 全10件」を維持（内訳部分だけ落とした）。
- **4（◆◇◊の廃止）完了**（ユーザー承認・2026-08-26実装）: チップ・凡例・週間統計・
  最重要/高/中/低セクション・低の索引・検索結果の見出し・**メール（mail-template.js の
  section）**のすべてから記号を除去。**「高」の文字に朱を移して色の合図を維持**
  （.sec .mark に文字が入り字面は .sec を継承・週間統計と凡例は .impch.hi／インラインで朱）。
  メールはサンプルreportでビルドし記号残存ゼロを機械確認。
- **5（タブ）完了: 案D（押しボタン型）×濃度4で実装**（ユーザー決定・2026-08-26）。
  非選択＝右下に墨のオフセット影で浮く（押せる）／選択＝影が消えて2pxめり込み＋
  **面が暗くなる（--ink-press=#4e4e4c・墨の濃度約80%）＋白抜き太字**。
  hover=浮いたまま#f6f5f1で少し陰る。経緯: 第1回モック（地色#f6f5f1＋太字）は
  「押している感覚にならない」で差し戻し→第2回モックで**墨の濃度5段階**を比較し4を選択
  （**押された面は周囲より暗い＝物理の直感。墨の濃度は色を増やすことにならない。
  対比が一目で分かることを最優先**——ユーザーの設計判断）。
  ⚠️濃度3(45%)が墨文字で読める上限・4からは白抜き必須。5=墨ベタは題字と真っ黒が並ぶため不採用。
  ⚠️影のぶん .views の padding 右2pxを確保している（削ると影が切れる）。
  検証: 切替・320pxで3枚1行（横スクロールなし）・固定ヘッダー86px不変。
  モックは両方削除済み（P29の作法）。

## 2026-08-26（P41・制度解説/Q&A/通知類の調査。**調査のみ・実装なし・提案の承認待ち**）

- 実測（履歴717件）: **制度文書語彙（通知/Q&A/事務連絡/解釈等）はタイトルに3件しか出ない**が、
  **介護保険最新情報（介護の通知・事務連絡・Q&Aの本流）はWAM RSSで既に月9本前後流れている**
  （タイトルが「Vol.NNNN（PDF）」のみで語彙に出ず、要約も一般論・リンクはPDF直）。
- ★ 発見: **WAM NETのアーカイブ（/gyoseiShiryou/detail-list?bun=020060090）は
  Vol＋件名＋通知日＋PDF直リンクのHTML表・全約1,475件**。件名は機械抽出できる
  （例: Vol.1537=「令和８年度地域支援事業実施要綱等の改正点について」）。
  WAM行政資料は高齢・介護/障害/子ども・家庭/福祉全般の分類DB（HTML表・件名つき）。
  ただし障害の報酬関連カテゴリはR3改定止まり——**障害・保育のQ&Aの現役集約は
  厚労省ページ（R6報酬改定・省令告示/通知事務連絡の節）と、こども家庭庁の
  「法令・通知等」各ページ**（題つきPDFの静的集約・新着には流れない型）。
- robots: wam=Amazonbotのみ禁止／mhlw=cgi-bin等のみ／cfa=標準除外のみ／
  **⚠️福岡県は /kodomo/ 配下が Disallow**（現監視パスは可・新源選定時に注意）。
- 福岡県: 指定関係通知（静的・条例＋国告示PDF）・集団指導（年1）・質問受付あり=ローカルルール源。
- **提案（承認待ち）: 第4の面は作らない。「検索の拡張＋原本への導線」の最小形**——
  ①WAM件名索引を検索対象に追加（＋紙面のVol項目に件名を補う）②サイド欄に「制度の原本」
  リンク箱（静的・台帳管理）③障害・保育の集約はリンク箱で原本誘導（パーサは作らない）。
  理由: 蓄積参照の実体はWAM/省庁の集約ページに既にあり、PDF不読の制約下で足せる価値は
  「探せる件名」と「導線」のみ＝面でなく検索と棚で足りる。
- 厳守の確認: jyosei-navi非アクセス・保留5源不変・CNAME設定に非接触。
- **[DECISION] 提案3点（検索拡張＋原本リンク箱＋作らない3つ）をユーザーが承認**（2026-08-26）。
  **作らない理由の記録（実装漏れではなく判断）**: ①第4の面——蓄積参照の実体（分類・検索つき
  アーカイブ）はWAM NETと省庁の集約ページに既に在り、締切も日付レールも無い情報は既存の面の
  構造に乗らず、作れるのは題名の複製棚だけ ②障害・保育の集約ページのパーサ——改定期集中型で
  平時の更新が少なく、ページごとのパーサ維持コストに見合わない（静的リンクで原本誘導すれば足りる）
  ③PDF解析——依存ゼロ構成の維持と幻覚リスク回避（P28の[DECISION]の踏襲）。
- **[DECISION] 区切り①（設計）をユーザー承認**（2026-08-26）: 件名索引=リポジトリJSON・毎朝1回
  全量置換・履歴は書き換えず表示時補完・検索は履歴/原本で束を分離・リンク箱は docs/genpon-links.md
  が正本・daily.ymlは巡回→索引→判定→送信の順・初期10本にe-Gov法令検索を含む。
- **WAM規約の確認結果**: `リンク著作権等について`（tyosakukenn.html・Shift_JIS）に記載あり——
  ①リンクは自由（トップ・個別とも・事前連絡不要）②リンクである旨を明示
  ③設定サイトのURLを webmaster2@wamnet.wam.go.jp へお知らせください（事後連絡の依頼）
  ④リンク以外の方法による引用・転載・複製は原則禁止（承諾制）。
- **[DECISION] 案A（WAMへ照会してから件名索引を実装）をユーザー採用**（2026-08-26）。
  理由: 県協会に見せる公共物で解釈を自分に有利に取らない／③でどのみち連絡が要る／
  待つ間にリンク箱は進められる。**照会文面は作成済み**:
  `~/Documents/fukushi-watch-taigai/wamnet-link-shokai-2026-08-26.html`
  （③のお知らせ兼用・未連絡だった事実とお詫びを明記・計画の詳細・差し支えあれば取りやめ・
  氏名/連絡先は差し替え）。**送信はユーザー作業→回答待ち。承諾後に区切り②（kenmei.js）を実装**。
- **区切り③のD（制度の原本リンク箱）を先行実装・公開**（2026-08-26・ユーザー承認の並行作業）:
  正本= `docs/genpon-links.md`（11本・全URL生存確認済み・WAMの2本はリンク自由の範囲）。
  `#boxGenpon`＝凡例の下・既定は畳み・3面共通・スマホはメニューへ移設（relocateに追加）・
  取得失敗時は箱ごと非表示。⚠️福岡県の集団指導（介護）は令和7年度ページが404になっており
  見送り（令和8年度ページが出たら台帳に1行足す——genpon-links.mdの注記）。
  ⚠️年度付きURL（障がい集団指導）と改定年度ページ（障害報酬）は年度替わりで差し替えが要る。

## 2026-08-27（P42・スケジュール実行の破棄への対処）

- **事象**: 08-27朝の定期実行が**失敗ではなく「発火しなかった」**（実行 #17＝08-26 07:26 JST が
  最後で、08-27 のスケジュール実行が履歴に存在しない）。手動実行 #18（workflow_dispatch）は
  Success（7m12s）で当日分を回収済み＝**Actions は無効化されていない**。
- **原因**: スケジュール枠の混雑による**その回の破棄**。cron は `"0 22 * * *"`＝JST 07:00 の
  **正時ちょうど**を指定していた。過去6回の実際の起動は 07:20/07:23/07:20/07:20/07:25/07:26 で
  **毎日20分以上の遅延**があり、正時が最も混む枠であることが実測で裏付けられた。
- **[DECISION] 対処（ユーザー指示・2026-08-27）**:
  ① **cron を正時から外す**: `"0 22 * * *"` → **`"17 22 * * *"`**（JST 07:17 相当）。
     理由と実測値は daily.yml のコメントに記載（台帳が唯一の置き場にならないように）。
  ② **Actions のバージョンを上げる**: `actions/checkout@v4→v5`・`actions/setup-node@v4→v5`。
     #18 の警告「Node.js 20 is deprecated／v4 が Node 24 で強制実行」への対応。
     **node-version の指定値 "22" は変更しない**（実行環境の Node は据え置き）。
  ③ **保険の2本目 cron（JST 08:17）は追加しない**——手順0の確認で
     **新着0件の日も「更新なし」メールを送る設計**（notify.js 冒頭に「生存確認になる」と明記）
     と判明したため、2本目を足すと**毎朝2通届く**。0件スキップを入れるには scripts の変更が
     必要で今回のスコープ外。⚠️将来2本目を検討するなら、先に「0件なら送らない」を実装するか、
     2本目だけ送信を抑止する形にすること。
- ★ **運用手順（メールが来ない朝）**: まず**スケジュールの破棄を疑う**（Actionsの実行履歴に
  その日の行が**無い**なら破棄・**あって赤なら**通常の失敗）。破棄なら Actions タブの daily を
  **手動実行（workflow_dispatch）で回収**できる。⚠️取りこぼしは起きない——状態コミットが
  送信より後にあるため、回収実行でその日の新着がそのまま送られる（P3の順序の設計が効く）。
- ★ 検証（2026-08-27）: daily.yml の構造を機械確認（タブ混入0・cron 1本＝`17 22 * * *`・
  uses は checkout@v5/setup-node@v5・node-version "22" 不変・8ステップの順序不変・
  状態コミットがメール送信より後）。

## 2026-08-27（P43・研修のPDFからの締切取得。**調査のみ・実装は承認待ち**）

- **対象**: 福岡県介護福祉士会（sources.md #21）の研修ページ。HTML側に開催日・会場・費用は
  あるが**締切が無く**、リンク先PDF（支部地区部会研修のお知らせ）にのみ締切が載る。
  福祉分野ではこの形が多く、今後も増える見込み。**締切は研修面で最も行動に効く欄**のため
  取得の価値が高い。
- **実測（1つのPDF・5ページ・16研修で検証・Chat側で実PDFを使用）**:
  - テキスト層あり（画像PDFではない）
  - 2段組。素の抽出では左右が混線し、**隣接行が別研修の締切になる** → 列で分ければ解消
  - 「開催要綱」で分割 → ブロック **16/16** が正しく切れた
  - 「《 締 切 》」の正規表現 → **16/16 抽出成功**。令和9年1月30日→2027-01-14 も正しく変換
- **[DECISION] 設計の骨子（実装時に守る）**:
  1. ⚠️ **1PDF＝1研修ではない**。PDF内のブロックを個々の研修に対応させることが本体。
  2. ⚠️ **題名を切り出さない**。副題を拾い、行の重なりで文字が混線する事故が実測で出た。
     HTML側の題名が**ブロック本文に含まれるか**で引く（突合の向きを逆にする）。
  3. ⚠️ **題名だけでは2件に当たる場合がある**。題名＋開催日の2条件で引き、
     **1件に絞れなければ締切を付けない**（助成の幻覚ガードと同じ思想。
     誤った締切は、無い締切より悪い）。
  4. Node実装では pdfjs-dist でテキストアイテムのx座標を取り、列を分ける
     （ページを幾何的に切るより正確）。追加費用はゼロ。
  5. ⚠️ **PDF本文は保存しない**。抽出した締切等の構造化された値だけを持つ
     （報道源・WAMと同じ原則）。
  6. ⚠️ 他源のPDFが同じ構造とは限らない（段組・テキスト層の有無・締切の表記）。
     1源で作り、**源ごとにパーサを差せる形**にする。
- **未着手の理由**: 対外5件の送付とHTTPS発行を優先。表示名の実装との並行を避けた。

## 2026-08-27（P44・源の「表示名」——団体欄を団体名だけにする）

- **[DECISION] 源の名前は識別キー。表示名を別に持つ**（ユーザー指示・2026-08-27）。
  ⚠️**「源の名前」列は1文字も変えない**——変えると①既定分野の名前引き（P36）が同名衝突で
  誤って倒れ ②`grants.js` の funder が `src.name` から生成され **grantHash の材料**のため
  **助成が全件再検知＝メール大量送信**になる（コードで確認済み）。
- **実装**: ①`sources.md` の表の**最後**に「表示名」列を追加（⚠️既定分野は `cells[7]`・
  表示名は `cells[8]`。左や途中に挿入すると読み取りが1つずれる）。空欄＝源の名前を使う。
  ②`crawl.js` が毎朝 `data/source-labels.json`（源の名前→表示名）を書き出す。
  ⚠️**状態に関わらず台帳の全26行**を出す（保留中の源の過去記事も正しく表示するため）・
  **巡回より前**に書き出す（巡回が失敗した朝でも表示は直る）。
  ③`index.html` は `srcLabel()` で引く。**表示だけの仕組み**。
- ★★ **判定・集計・検索・リンク・ハッシュは源の名前のまま**。とくに `sealHtml` の印章判定に
  表示名を渡してはいけない——実例:「WAM助成（福祉医療機構）」を表示名「福祉医療機構」で
  判定すると **［独］が［国］に化ける**。「全社協 福祉の助成情報」→「全社協」でも
  ［団］判定の源名側の保険が消える。**変えるのは画面に出る文字だけ。**
- 差し替えた描画は7箇所（sealHtml＝紙面/助成/研修の3面が経由・**低の索引**（lowLine・
  sealHtmlを通さない直書き。ユーザー承認で追加）・報道欄・報道ティッカー）。
  **検索の対象文字列は `source` のまま据え置き**（ユーザー判断・長い名前でも当たる）。
- ★ 検証（2026-08-27・機械確認）: 対応表26行＝台帳と一致・空値0・短縮14行が指定どおり・
  保留中の源も含む／**crawl.js 実行前後で hash・fields・source・funder の変化が全て0件**
  （grants 10・kenshu 56・history 292＝計358項目を照合。grants.json/kenshu.json は差分なし）／
  3面の団体欄が短縮され**印章は［県］［独］［国］［団］［報］が維持**／低の索引も短縮／
  検索は長短どちらの語でも当たる（31件・68件）／PC1280・スマホ375で86px不変・横スクロールなし。

## 2026-08-27（P45・外部レビュー8件の照合と実装。**③は実機確認を経て現行維持で決着**）

- ★★ **教訓（この案件で最も高くついた種類の誤り）: 実測値を「その表現をやめる根拠」に使わない。**
  外部レビューの「急ぎの締切が淡く見える」に対し、明滅の谷で朱が 1.44:1 まで落ちることを
  実測し、**谷を .88 へ上げた（＝事実上の静止化）**。しかしこれは
  `[DECISION] 明滅は2秒周期・濃度1⇄0.25に（ユーザー指示の「2倍の速さ・2倍の濃さ」）` を
  台帳未確認のまま反転させる変更だった。コミットは破棄した。
  ⚠️**外部の指摘を実装する前に、その挙動を選んだ[DECISION]が台帳にあるかを必ず引く。**
  コードのコメントを読んで「意図的に見える」で代用しない（⑧では逆に、コメントから
  「意図的な使い分け」と断定したが台帳に記録は無かった＝根拠のない断定だった）。
  数値は「表現をやめる根拠」ではなく「**表現を保ったまま何を動かすか**」の制約として使う。
- **照合の結果（8件＋2件）**: 台帳に意図の記録が**あった**のは ③明滅・④内側スクロール・
  ⑤サイド欄の順序・⑦空振りの文字（3件とも）・タブの押し込み の5件で、いずれも
  **[DECISION] かユーザー指示**。**記録が無かった**のは ②金額の単位・⑧用語・
  要約本文のコントラスト目標、および①の右カラム(gStat)の数え方。
- **[DECISION] 「ラベルは最小限（予想できる語は書かない）」と「記載なしは文字として残す」は
  矛盾しない。** 前者が削るのは**予想できる語**であり、「記載なし」は予想できない情報
  （省くと「制限なし」と誤読され、P28が警告した実害そのものになる）。以後この基準で判定する。
  → 一般原則の側（生きている注意事項）にも判定基準を追記した。
- **[DECISION] 「締切」＝先方が締め切る日／「期限」＝こちらが動く目安。**
  語は統一しない（対象が違うため）。3面共通の「記号の見かた」に定義を1行だけ置く。
- **① 助成の内訳（gStat）を見出し(gCount)と同じ変数から数えるようにした。**
  旧実装は `state.grants`（全件）を数えており、絞り込み中に**同じラベルで違う数**が並んだ。
  さらに受付前を「締切あり」に含めており `[DECISION] 受付前を別の束にする`（P34-2）に反していた。
  ⚠️**右カラムに「全N件」は出さない**——`[DECISION] 「／ 全N件」の後置は分野・用途で
  絞ったときだけ`（P31②）があり、同じ条件で見出しが既に出しているため二重になる。
- **② 金額を表示側で万円に統一した**（`manYen()`・原文は title 属性に保持）。
  ⚠️**grants.js は触らない**: ①amount は「原文のまま短く」取る設計で原本突合の拠り所
  ②取り込み側を直しても保存済みの項目は次の巡回まで千円のまま残る ③表示側なら過去分も即座に揃う。
  grantHash は funder+title+url のみで **amount を含まない**ため再検知＝メール再送は起きない。
- **--faint（極薄墨 2.42:1）はリンクだけを薄墨へ。** `.orig a` と `.press-item h4 a` の下線の
  2箇所のみ。⚠️**値と他26箇所は不変**。配色の変更ではなく
  `[DECISION] ①移動するリンク（墨・薄墨）→紺になる`（P23）で**平常時のリンクは墨・薄墨**と
  定義済みであり、極薄墨のリンクはその集合の外＝**体系の適用漏れの是正**。
  ラベル側（`.orig`）は極薄墨のままで「リンクが主・ラベルが従」の濃淡は維持。
- **③ 明滅は実装せず `mock-p45.html` を作った**（案A＝文字は朱のまま静止・手前の印だけを
  1⇄0.25・2秒で明滅）。台帳の数値をそのまま印へ移すので**ユーザー指示が保存される**。
  モックで選ぶのは**印の形（●／▲／枠つき「急」）・大きさ3段階・間隔3段階**の3つだけ。
  受付前は印を出さない／reduced-motion で静止、も両方モックに入れてある。
  ⚠️**印の大きさと間隔は文章では決まらない**（dev-workflow 7-1b）。実機で選んでから実装する。
- **[DECISION] 締切の明滅は現行のまま（文字が明滅）を維持**（2026-08-27・モックの実機確認を
  経たユーザー決定）。案A（文字は静止させ朱の印だけを明滅）は**不採用**。
  ⚠️**谷のコントラスト 1.44:1 は、この決定の下では「直すべき欠陥」ではない。**
  明滅は「離れていても気づく」ために入れた表現であり、**文字の濃度が動くこと自体が合図**。
  次にこの数値を見つけた者が再び「直そう」としないよう、ここに記録する。
  → 決着したので `mock-p45.html` は削除した（P29・P40と同じ作法）。
- ★★ **教訓の再確認: 実測値は「その表現をやめる根拠」ではなく、「その表現を保ったまま
  何を動かすか」の制約として使う**（P45で2度確認した——1度目は谷を.88へ上げた誤り、
  2度目は案Aの提示。案Aは筋としては正しかったが、**そもそも直す必要がなかった**）。
- **触っていない**: ④内側スクロール・⑤サイド欄の順序・⑥研修の件数・タブの押し込み・
  要約本文のグレー（いずれも台帳の決定どおりで妥当と確定）。scripts・data・sources.md・CNAME も不変。
- ★ 検証（2026-08-27・Chromium実測）: 見出しと右カラムが**分野4通り（高齢/障害/保育/全）すべてで一致**・
  既定状態にも絞り込み中にも右カラムへ「全N件」は出ない／千円の残存**0件**（6件中4件を換算）／
  `.orig a` 2.42→**4.72:1**・`.orig`/`.tagline`/`.sec .l`/`.low-h` は極薄墨のまま不変・
  `var(--faint)` の使用は28→**26**（値 #a8a49d は不変）／用語の1行が3面とも4.72:1で表示／
  3面の描画・**JSエラー0件**・PC1280とスマホ375で**固定ヘッダー86px不変・横スクロールなし**／
  モックは3形とも**文字の animation が none**（＝文字は静止）で受付前の行の印は0個。

---

# ロードマップ

> **P0〜P25 の行は `fukushi-watch-archive-2026-08.md` の「ロードマップ P0〜P20」「同 P21〜P23」
> 「同 P24〜P32・P34」へ原文のまま移設した**（文字数の維持規則による）。
> **どのフェーズで何をしたかを辿るときはそちらを見る。**

| フェーズ | 内容 | 完了条件 | 状態 |
|---|---|---|---|
| **P33** | アイコン設置（favicon/apple-touch-icon/最小manifest）＋カスタムドメイン fukushi-watch.fknd.jp への移行 | アイコンは公開URLで配信確認。**ドメインはXserverゾーン有効化待ちで保留**（待ち明けの作業は記録の⏸を見よ） | ⏸ 1完了・2はHTTPS発行待ち（2026-08-27・配信は新ドメインで復活済み） |

### P6 の設計方針の候補（P6の区切り①で検討する。今は実装しない）
- **分野は「絞り込み」ではなく「重要度の重み付け」に使う。**
  選んだ分野外を除外すると、報酬改定・虐待防止など**全分野に効く通知を取りこぼす**ため、
  関係の薄いものは低に落として残す。
- **設定の置き場所**（リポジトリ内のJSON ／ P4のダッシュボードにUIを置く）は、
  **P4 の設計と合わせて決める**。
- **意義**: 判定の前提が可変になれば、高齢分野の法人や**県協会の会員施設にもそのまま配れる
  ツール**になる（台帳冒頭の「昇格前提」と直結する布石）。

---

# 経緯アーカイブ

`fukushi-watch-archive-2026-08.md` にある（P0〜P28（P24の枝番・P25の後を含む）の記録・ロードマップP0〜P32・P34の行。原文のまま保存）。
記録が10件を超えたら、**完了済みの記録のうち**最も古い1件を同ファイルへ
原文のまま移してから追記する（進行中の記録は飛ばす）。
⚠️ 字数の判定は文字数で行う（`wc -c` はバイト数＝日本語で約2.3倍に出る）。
