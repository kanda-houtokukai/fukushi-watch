# 福祉情報ウォッチ — 引き継ぎ台帳（handoff）

> 引き継ぎの入口はこのファイル1本。新しいチャットは、まずここを読んでから作業を始める。
> 二層運用: ①現在地サマリ（毎回読む）②経緯アーカイブ（必要なときだけ読む）。

---

# 現在地サマリ

## この案件は何か

行政（こども家庭庁・厚労省・自治体等）の福祉関連情報を**毎日巡回して更新を検知し、
要約と重要度判定を付けて毎朝メールで届ける**監視ボット。
**巡回 → 差分検知 → 要約 → 重要度判定 → 通知**の5役が連携するマルチエージェント構成。
目的は①実用ツール（毎朝メールで更新を受け取る）②マルチエージェント設計の学習、の2本立て。

## 現在地

- **P13（検索例＋セクション見出しの階層明示）まで完了（2026-08-18）。実運用中＋一般公開中。**
- P12: 「このサイトについて」から「個人開発の」を削除。共有は Web Share API 対応
  （スマホ=端末の共有シートでタイトルとURLのみ／キャンセルは無表示／PC=URLコピー。
  アイコンは鎖→共有の形）。
- P13: 検索例「例: 研修 申込」（両入力欄）。セクション見出しを記事と別系統の字面に
  （11.5px・字送り.22em・グレー＋境界2px太罫＋前余白44px/スマホ34px。色は墨朱2色のまま）。
  報道の柱も同体系。
- **P11（締切ウォッチ＋共有リンク）完了（2026-08-18）。**
- 締切ウォッチ: 当日の高・中のみ原本本文を取得（間隔1.5秒・本文は破棄）し、
  項目ごとの小リクエストで期限を抽出。**幻覚ガード三重検証**（quote実在／quoteからの
  日付機械再抽出一致／今日〜2年先）を通った期限だけ `data/deadlines.json` に保存
  （期限・種類・根拠文字列・hash のみ）。期限切れは保存からも自動削除。
  表示: 「期限が近いもの」欄（PCサイド欄=検索の直後／スマホメニュー先頭・残り日数・
  行タップでジャンプ）＋期限確定項目のメタ行に控えめ表示。
- 共有リンク: 全項目（低・報道含む）に鎖アイコン。`?d=YYYY-MM-DD&i=hash8` をコピーし、
  開くと該当日の紙面の該当項目へ自動スクロール＋1.6秒ハイライト。
- **P2以来の「本文への追加巡回の見送り」はP11で解禁**（高・中のみ・締切抽出目的に限定）。
- P9の状態は継続: 福祉新聞は照会待ちで保留・報道は介護ニュースJointのみで運用。
- スマホ(900px未満): 欄外＋題字を上部固定（position:fixed＋本文にpadding-top:96px・
  高さ86px一定・スクロールで題字のみ縮小、紙面は跳ねない・日付移動で紙面先頭へ自動スクロール）。
  右端に検索（スライド開閉）とメニュー（右差し込みドロワー）。
  メニューの中身: この7日→今週の記録→記号の見かた→このサイトについて（折りたたみ）。
  分野チップは紙面（日付見出し直下）に残す。
- PC(900px以上): サイド欄をメニューと同順に再編
  （検索→分野→この7日→今週の記録→記号の見かた→このサイトについて（折りたたみ・本文下から移動））。
- 実装方式: **DOMノードを幅に応じて移設**（描画ロジック・イベントは共有・複製なし）。
  matchMedia change が飛ばない環境向けに resize でも relocate（冪等）。
- P9の状態は継続: 福祉新聞は照会待ちで保留・報道は介護ニュースJointのみで運用。
- 報道の扱い（確定方針の実装済み）: 行政と別区分（kind: press・diff/report/履歴とも別配列）・
  AI要約・重要度判定なし（分野タグのみ第3リクエストで付与・出力はindexとfieldsだけ）・
  見出し/リンク/日付/カテゴリのみ保存（RSSの説明文・本文は読み捨て）・
  グラフと件数に不算入・〔報〕印章と注記を常設。
- **福祉新聞は sources.md の状態「保留（照会中）」**: 「見出しの無断転載禁止」の明記があるため
  編集部へ一報し回答を待つ（ユーザー決定）。パーサとコードは残してあり、
  **状態を「巡回中」に戻すだけで復帰**する。問い合わせ文面案は作成済み（ユーザーが送付）。
- ダッシュボードは「新聞紙面」構成: 題字（二重罫）・欄外（和暦日付・創刊から第N号=
  2026-08-16起点）・PC2カラム（本文＋サイド欄: 検索/この7日/今週の記録/分野/凡例）・
  スマホ1カラム（上部圧縮版: 題字縮小・副題/月ナビ/重要度チップ非表示・矢印移動）。
- グラフ「この7日」は**対応が要る日の地図**（高だけ朱・中低は無彩色・高の日は朱点・
  タップでその日の紙面へ）。検索は直近3か月→3か月ずつ遡る方式（範囲明記）。
- AIの働きかけ: 高の項目に**対応の目安**（第2リクエストで生成・タップ展開・
  「一般的な目安です。最終判断は必ず原本を」常設）、全項目に**関連する過去の記録**
  （系列キーの機械照合・API消費ゼロ）。
- 出典の印章: 国=黒塗り角/独法=白抜き角/県=丸/**報=破線丸（CSSのみ・P9で使用）**。
  分野下線色: 保育=山吹/児童=梅/障害=藍/高齢=松葉。並びは全箇所 保育・児童・障害・高齢。
- **mock/ は撤去済み**（P7の保留分。P8承認後に削除）。
- 日跨ぎ履歴は実運用で実証済み（8/17朝の自動発行=第2号・夕方の手動実行で計7件）。
- システムの全体像: 毎朝7時（JST）に GitHub Actions が
  **巡回（5源）→ 差分検知 → Gemini要約・重要度・分野タグ → メール送信（分野重み付け）→
  履歴蓄積 → 状態コミット** を自動実行。
  - メール: 高◆中◇低◊構成・件名に件数と最高重要度・更新なしの日も生存確認メール
  - ダッシュボード: **https://kanda-houtokukai.github.io/fukushi-watch/**（GitHub Pages・
    日報型＋日付レール・重要度/分野の切替・匿名運用）
- P4公開の検証済み: 公開URLで実データ表示・Actions2回で履歴の冪等性確認・
  法人特定情報0検出（機械チェック）・0件/大量日の表示・スマホでユーザー承認済み。
  ※日を跨いだ履歴の積み上がりだけは翌朝の実運用で自然に実証される。

## 次の一手

- **福祉新聞への問い合わせ送付（ユーザー作業）→ 回答を待つ。**
  掲載可の回答が来たら: `docs/sources.md` の福祉新聞行の状態を「巡回中」へ戻すだけ
  （コード変更不要・stateのベースラインも温存済み）。不可なら行を「掲載不可」に更新して終了。
- 上記以外は指示書待ち（停止中）。
- 観察ポイント（1〜2週間の実運用で見る）:
  ①メール分量の体感 ②「高」インフレ（P6の中立化で改善傾向。経過観察）
  ③分野タグの精度（特に「共通」の拾われ方）④対応の目安（action）の距離感と品質
  ⑤related が履歴の成長とともに増えるか（系列照合は当月+前月を見る）。

## 生きている注意事項

- ★ **鍵類の値を、リポジトリ・台帳・チャットに絶対に書かない。** Gemini API キーと
  Gmail アプリパスワードは **GitHub Secrets にのみ**保管する。文書に書いてよいのはシークレット名まで。
- ★ **運用コストは完全ゼロを維持する**（Gemini API 無料枠・GitHub Actions 無料枠）。
  課金が発生する選択肢は、採用前に必ずユーザーに確認する。
- ★ **差分スナップショットに監視先ページの全文をコピーして保存しない。**
  将来にわたって、保存するのは**ハッシュ＋見出し等の最小限**にとどめる。
  理由: ①官公庁ページの全文保存は著作権上の配慮が要る ②全文を貯めるとリポジトリが肥大し、
  差分の確認そのものが重くなる ③変更の検知に必要なのは「変わったか」と「どこが変わったか」であり、
  本文の複製ではない。**原本への直リンクを常に残し、最終判断は原本確認に誘導する**（dev-workflow 鉄則5）。
- ★ **P0ではコードを書かない。** 器（フォルダ・台帳・CLAUDE.md）だけを作る。
- ★ **外部の鍵が要る P2（Gemini APIキー）・P3（Gmail アプリパスワード）は、指示書の冒頭に
  「手順0」を置く**（設定の雛形を作る → 入手先を案内する → いったん停止して待つ）。
  ユーザーに準備を先回りさせない。
- ★ `docs/sources.md` の**確定済みの行は承認なしに書き換えない**。URLの確定はP1（1源目）・P4（残り4源）。
- ★ 報酬改定関連は **報酬改定ナビ**（`~/Documents/houshu-navi/`・kanda-houtokukai/houshu-navi）と
  情報源が重なる可能性がある。突き合わせて重複監視を避ける。
- ★ **本案件は「個人の試験開発」**。法人PT案件ではないため `references/pt-context.md` は適用しない。
  ただし**形になったら PT・県協会への提示物に昇格**する想定なので、他人が見る前提の作りにしておく。
- ★ **停止規律（P9の教訓・2026-08-18）**: 指示書に「区切り②は区切り①の承認後のみ」とある場合、
  **その区切りの承認を明示的に確認してから次へ進む**。選択肢の提示に推奨を添えて一括承認を得る形は、
  判断を先導して停止ポイントを実質すり抜けることがある。特に**著作権・対外関係など
  実装の妥当性と別軸の判断**が含まれる区切りでは、論点ごとに承認を分けて確認する。
- ★ **メールの分量（P5で実測・承認済み・引き続き観察）**: 5源の実測は**平日12〜18件/日**
  （当初懸念の30〜50件より少ない）。15件のメールをユーザーが確認し「このまま運用」で承認済み。
  それでも長いと感じたら: **高・中はメール本体として現状維持、低は圧縮**（件数の集約のみ等）。
  低の詳細は P4 ダッシュボードの役割とし、**メール＝判断が要るもの／ダッシュボード＝全件**の分担。
- ★ **「高」判定のインフレ傾向（観察中・P4以降で判断）**: 感染症の**定期更新データ**
  （コロナ定点報告の推移など毎週来るもの）まで「高」に判定される。基準の「感染症=高」に忠実な
  結果だが、毎週高が並ぶと件名の「高N件」が薄まる。1〜2週間の実運用で頻度を見て、
  プロンプトに「定期更新の統計データは中」等の微調整を検討する。

## ブロッカー

- なし（P1指示書待ちの停止中）。

## ファイルの地図

| ファイル | 役割 | 正本か |
|---|---|---|
| `fukushi-watch-handoff.md` | この台帳。引き継ぎの入口 | ✅ 正本 |
| `CLAUDE.md` | Code がセッション開始時に自動で読む案件固有ルール | ✅ 正本 |
| `docs/sources.md` | 監視対象台帳（源の名前／URL／巡回方法＝パーサ名／状態） | ✅ 正本 |
| `scripts/crawl.js` | 巡回＋差分検知（P1）。台帳の「巡回中」行だけを巡回。依存ゼロ | ✅ 正本 |
| `scripts/summarize.js` | Gemini要約＋重要度判定（P2）。diff→report。依存ゼロ | ✅ 正本 |
| `data/state.json` | 前回巡回時の項目リスト＋`preferredModel`（成功したGeminiモデル） | 実行で更新 |
| `data/diff-latest.json` | 直近実行で検出した新規項目。**P2の入力** | 実行で更新 |
| `data/report-latest.json` | 要約＋重要度付きの新規項目。**P3のメール本文の材料** | 実行で更新 |
| `.env.example` | 必要な環境変数の雛形（`GEMINI_API_KEY=`・値は空） | ✅ 正本 |
| `.env` | 鍵の実値。**git追跡外**（.gitignore済み）。値はどこにも書かない | ローカルのみ |
| `scripts/mail-template.js` | 件名とHTML本文の組み立て（P3）。P4ダッシュボードも同じ材料を使う | ✅ 正本 |
| `scripts/notify.js` | Gmail SMTPS(465)送信（P3）。依存ゼロの自作クライアント | ✅ 正本 |
| `.github/workflows/daily.yml` | 毎朝7時(JST)=22:00(UTC)の定期実行＋手動実行 | ✅ 正本 |
| GitHub Secrets | GEMINI_API_KEY / GMAIL_USER / GMAIL_APP_PASSWORD / MAIL_TO の4つ | 値はGitHubのみ |
| `scripts/archive.js` | 履歴蓄積（P4）。report を月次＋索引へ。同日はハッシュ併合 | ✅ 正本 |
| `data/history/` | `YYYY-MM.json`（日毎の全項目）＋`index.json`（日毎件数） | 実行で更新 |
| `data/mail-settings.json` | メールの分野重み付け設定（現在=障害・児童・保育） | ✅ 正本 |
| `index.html` | ダッシュボード本体（依存ゼロ・単一ファイル） | ✅ 正本 |
| https://kanda-houtokukai.github.io/fukushi-watch/ | 公開ダッシュボード（Pages・main/root） | — |
| `package.json` | `npm run crawl` の定義のみ（依存なし） | ✅ 正本 |
| `~/Documents/fukushi-watch/` | ローカルの作業場所 | — |
| `github.com/kanda-houtokukai/fukushi-watch` | 公開リポジトリ | — |
| `~/.claude/skills/dev-workflow/` | 作業規範（全案件共通） | ✅ 正本 |

> P2以降に追加される予定（まだ無い）: 要約・重要度判定スクリプト（P2）、
> メール送信＋`.github/workflows/`（P3で定期実行を導入）。

---

# 決定事項（[DECISION]）

## 2026-08-16（Chat確定・P0）

1. **[DECISION] 案件は「行政の福祉関連情報の監視ボット」。名称は福祉情報ウォッチ、
   リポジトリは `kanda-houtokukai/fukushi-watch`（公開）、ローカルは `~/Documents/fukushi-watch/`。**
   理由: 報酬改定ナビ（同 owner・`~/Documents/houshu-navi/`）と配置規則を揃え、
   raw での直読み（dev-workflow §8）を可能にするため。
2. **[DECISION] 立ち位置は「個人の試験開発」。形になったら PT・県協会への提示物に昇格させる。**
   理由: 現段階は学習と実用の私的な試作であり、法人PTの成果物として抱え込ませない。
   ただし昇格を前提に、他人が読む水準のドキュメントを最初から維持する。
3. **[DECISION] 目的は2つ。①更新情報を毎朝メールで受け取る実用ツール ②マルチエージェント設計の学習。**
   理由: 実用性だけを追うと学習目的の構成（役割分担）が崩れ、学習だけを追うと使われないため、
   両方を明示して以後の設計判断の基準にする。
4. **[DECISION] 構成は「巡回 → 差分検知 → 要約 → 重要度判定 → 通知」の5役連携。**
   理由: 決定3の②に直結する設計。役を混ぜず、1役=1責務で分ける。
5. **[DECISION] 実行基盤は GitHub Actions の定期実行（毎朝7時・変更可）。**
   理由: 追加コストゼロ・サーバ管理不要で、リポジトリに状態を置く構成と相性がよい。
6. **[DECISION] 状態保存はリポジトリ内 JSON（DBなし構成）。**
   理由: 報酬改定ナビで実績がある。DBの契約・管理コストが不要で、差分の履歴が git で追える。
7. **[DECISION] AI は Gemini API の無料枠（自分側名義）。**
   理由: 決定10（運用コスト完全ゼロ）を満たす。名義を自分側にすることで、
   法人の契約・承認を待たずに試作を進められる。
8. **[DECISION] メールは Gmail SMTP（アプリパスワード）。鍵類はすべて GitHub Secrets に保管し、
   チャット・台帳・リポジトリに値を書かない。**
   理由: 追加コストなしで確実に届く経路。鍵の値を文書に残すと、公開リポジトリでは即座に漏洩する。
9. **[DECISION] 監視対象は `docs/sources.md` の台帳方式。初期5源＝こども家庭庁／厚労省／
   WAM NET／福岡県／報酬改定関連。P0では行だけ用意し、URLの調査・確定はP1以降。**
   理由: 情報源の増減は運用の中心的な変更点であり、コードに埋め込むと変更のたびに実装が要る。
   台帳を先に正本化しておけば、以後は**台帳に1行足すだけ**で監視源を増やせる（P4の設計）。
10. **[DECISION] 運用コストは完全ゼロ（すべて無料枠内で運用する）。**
    理由: 個人の試験開発であり、コストが発生した時点で継続の判断が要る。
    無料枠に収まる構成（決定5〜8）を選ぶ制約として先に固定する。

## 同日・P0の作業方針として確定（上記の補足）

- **[DECISION] P0のスコープは足場作りのみ。コードは書かない。**
  理由: 監視対象のURLも巡回方法も未確定な段階で実装に入ると、手戻りが実装ごと発生するため
  （dev-workflow 鉄則2・7-1）。
- **[DECISION] 差分スナップショットは全文コピーを保存せず、ハッシュ＋見出し等の最小限にとどめる
  （将来にわたる方針）。**
  理由: 「生きている注意事項」に記載のとおり（著作権への配慮・リポジトリ肥大の回避・
  検知に必要な情報だけを持つ）。
- **[DECISION] 台帳の維持規則は「版/フェーズの記録は直近10件・3.2万字」。超過分は原文のまま
  月別アーカイブへ移す（要約による削減は禁止）。**
  理由: 台帳は毎セッション読まれるため、肥大がそのままコンテキストの浪費になる（dev-workflow 鉄則6）。

## 2026-08-16（P1）

- **[DECISION] こども家庭庁の監視は `/news` の全カテゴリ（1リクエスト）。機械では絞らない。**
  理由: カテゴリ絞りは P2 の重要度判定（Gemini）の仕事と重複し、機械で固定的に絞ると
  例外（別カテゴリに載る重要情報）を永久に取りこぼす。カテゴリ名は項目に保存して
  P2 の判定材料に渡す。ノイズが気になったら P2 側で調整する。
- **[DECISION] 項目ハッシュは「タイトル＋日付＋URL」で作る（URLだけにしない）。**
  理由: こども家庭庁は同一URLへ繰り返し掲載する（例: 人事異動は毎回 `/about/jinji`）。
  URLだけだと2回目以降の掲載を検知できない。★調査で発見した実仕様。
- **[DECISION] 依存ゼロ構成（cheerio 不使用・正規表現で抽出）。**
  理由: 対象が静的で規則的なHTMLであること、抽出0件ガードで構造変化に必ず気づけること、
  P3 の GitHub Actions でも `npm install` ステップが不要になること。
  ※新しい源のHTMLが複雑だったら、その時点で cheerio 導入を再検討してよい。
- ★ 教訓: 抽出0件は「成功・差分なし」でなく**構造変化の疑いとしてエラー終了**させる
  （サイレント0件が最も危険）。`crawl.js` に実装済み。
- ★ 調査メモ: こども家庭庁に RSS なし。sitemap.xml はサイト全体混在で監視には不向き。
  robots.txt は `/news` 許可。一覧は静的HTML（JSレンダリング不要）・1ページ10件・
  `?category=2〜11` の10カテゴリ・`?page=0〜` のページング。

## 2026-08-16（P2）

- **[DECISION] 重要度は3段階＋一行理由。ユーザー承認済み。**
  高=対応・確認の行動が必要になりうる（報酬改定・基準/法令改正・義務化・監査/指導・
  加算/補助金と期限・虐待防止/安全/感染症・パブコメ）／中=行動不要だが把握しておくべき
  （審議会/検討会・調査/統計・ガイドライン案・方針文書）／低=影響が薄い
  （人事・調達・採用・行事・広報）。**迷ったら「中」に倒す**（高が薄まるとメールが
  読まれなくなり、低に倒すと取りこぼすため）。判定材料は一覧のタイトル＋カテゴリ＋日付のみ。
- **[DECISION] Geminiのモデル名は固定で書かない。** 一覧取得 → 安定版flash系
  （`gemini-<版>-flash(-lite)`のみ・preview/tts/image等は除外）を新しい順に試行 →
  成功したモデルを `state.json` の `preferredModel` に記録して次回優先。
  世代交代404（gemini-2.5-flash の実例）への構造的な対策。
- **[DECISION] API呼び出しは全項目まとめて1リクエスト**（無料枠の節約）。
  件数が増えて1リクエストに収まらなくなったら、そのとき分割を検討する。
- ★ 教訓: Geminiは項目数を勝手に減らして返すことがある前提で、**入力件数と出力件数の
  一致を必ず検証**し、不一致は失敗扱いにする（summarize.js に実装済み）。
- ★ 教訓: APIエラー時に空の要約で正常終了させない。失敗は失敗として終了し、
  P3では「メールが来ない日=失敗に気づく日」にならないよう通知設計で拾う。

## 2026-08-16（P3冒頭）

- **[DECISION] 出口は通知（メール）とダッシュボード（Web）の両方を作る。**
  理由: メールアドレスを持たない職員がいること、プレゼン時にダッシュボードが有効なこと。
  ロードマップを P3=メール＋定期実行／P4=ダッシュボード（GitHub Pages）／P5=監視源の拡張
  に更新。どちらも P2 の `report-latest.json` を共通の材料にするため、
  **データの二重管理は発生しない**（出口が増えても材料は1つ）。

## 2026-08-16（P3）

- **[DECISION] SMTPは依存ゼロで自作**（node:tls で smtp.gmail.com:465 に直接接続・承認済み）。
  理由: P1の依存ゼロ方針と一貫し、Actionsで npm install が不要。文字化け等のリスクは
  実受信ゲートで検証して潰した。
- **[DECISION] メール構成（承認済み）**: 件名=件数＋最高重要度（例「高2件を含む新着7件｜
  福祉情報ウォッチ 8/16」）／本文=高◆→中◇→低◊ の順・低はタイトル1行に畳み
  ［カテゴリ・源名］を添える／原本リンクは常に独立行／新規0件の日は「更新なし」の短いメール
  （生存確認を兼ねる）／テーブル組＋インラインCSSのみ（Gmailは外部CSS・Flexbox・Grid不可）。
- **[DECISION] ワークフローの順序は 巡回→判定→送信→状態コミット。コミットを送信の後に置く。**
  理由: 送信に失敗した日は状態が更新されず、翌日の実行で同じ新着を再検知して再送する
  （失敗しても取りこぼさない構造）。
- ★ 教訓: **Gmail SMTP の認証には、そのGoogleアカウントに Gmail アドレス（Gmailサービス）が
  存在することが必要**。ログインIDがYahoo!等の外部アドレスのアカウントでは、アプリパスワードが
  正しくても 535 BadCredentials になる。切り分けは curl の SMTP機能（別実装）と突き合わせ、
  自作コードの問題でないことを先に確定させると速い。
- ★ 教訓: **Personal Access Token で `.github/workflows/` を push するには `workflow` スコープが
  必要**（`repo` だけでは拒否される）。トークンは値を変えずにスコープを後から追加できる。
- ★ 仕様: **Actions の cron は指定時刻ちょうどには動かない**（数分〜十数分の遅延が正常）。
  「7時を過ぎたのに来ない」はまず遅延を疑い、Actionsの実行履歴で確認する。

## 2026-08-16（P5）※ユーザー決定によりP4より先に実施

- **[DECISION] 巡回は5口構成**: こども家庭庁 `/news`・厚労省 `/stf/new-info/`・
  WAM NET 行政資料RSS・福岡県 `/life/3/27/`（障がい福祉）・`/life/3/39/`（子ども・青少年）。
- **[DECISION] 報酬改定関連は独立した源として不採用**（他源でカバー）。
  報酬改定ナビは静的知識ベースで新着の流れる場所ではなく、報酬改定の通知・Q&Aは
  厚労省＋WAM＋こども家庭庁で完全カバー。独立URLは二重検知になるだけ。行は経緯として残す。
- **[DECISION] 厚労省は全省庁横断の新着（平均9件/日）をそのまま採用し、選別はAIに任せる**
  （障害福祉に絞った新着一覧が存在しないため。P1の「機械で絞らない」方針と同じ理屈）。
- **[DECISION] 福岡県は2行に分割**（障がい福祉／子ども・青少年。同一構造でパーサ共通）。
- **[DECISION] 1源の失敗で全体を止めない**: 源ごとに try/catch、失敗は
  `diff-latest.json` の `crawlErrors` に記録して続行。**全滅のみエラー終了**。
  失敗した源の既存 state は変更されない（次回成功時に差分としてまとめて検知される）。
- ★ 調査メモ: 厚労省はHTMLコメント `<!--YYYYMMDDHHMM [カテゴリ] 絶対URL タイトル-->` が
  一覧の機械可読な複製になっており、これを抽出している（消えたら0件ガードが検知）。
  WAM NET はHTMLページがShift_JISだが**RSSはUTF-8**。福岡県は robots.txt が
  **`/rss/` と `/kodomo/` を Disallow**（RSS不可・ただし `/life/` `/contents/` は許可）。
- ★ 教訓: 状態をリポジトリに置く構成では、**ローカルでの検証実行と Actions の自動コミットが
  衝突する**ことがある（data/ の lastCrawled 差分）。ローカルの未コミット data/ は
  `git checkout -- data/` で捨てて Actions 側を正とすれば安全（Actionsの方が新しいため）。

## 2026-08-16（P6・P4公開より先に実施）

- **[DECISION] 分野タグの語彙は5値**: 保育/障害/高齢/児童/共通・複数可・該当なし=空配列。
  「共通」=分野を問わず福祉事業の運営に効くもの（虐待防止・処遇改善・感染症・災害等）。
  **迷ったら空でなく共通に倒す**（分野で絞ったとき重要情報が消える事故の防止）。
- **[DECISION] 重要度は「分野中立の1つ」＋分野タグの組み合わせ**（分野ごとの重要度は不採用:
  出力4倍でJSON崩れリスク増・件名の「高N件」が作れない）。プロンプトの判定前提を
  「障害福祉・児童発達支援・保育の法人」から「福祉事業者一般」へ変更した。
- **[DECISION] メール側は `data/mail-settings.json` の fields（現在=障害・児童・保育）で
  重み付け**: 設定と交わらない分野限定の項目だけ低へ降格。共通・分野なしは降格しない。
  降格はメモリ上のみで report-latest.json に書き戻さない（履歴は分野中立を保つ）。
  設定が無い/空/壊れていれば全分野扱い（メールを止めない）。
- **[DECISION] 25件超は複数リクエストに分割**（出力肥大による件数抜け・JSON崩れの構造対策）。
  fields の語彙検証（5値のみ・配列必須）も追加。
- ★ 検証: 実データ13件で「感染症→共通」「介護→高齢」「電離放射線障害・感染症対策課の採用
  →なし（文字面に釣られない）」を確認しユーザー承認済み。既存履歴（8/16=0件）は項目が無く
  移行不要。**読み手は fields 欠落を空配列として扱う**のが実装規約。
- ★ 観察: プロンプトの分野中立化で**「高」インフレが改善**（コロナ定点報告が高→中）。
  P5からの観察項目は継続するが、方向は良い。

## 2026-08-16（P7）

- **[DECISION] レイアウトの統合案**: 案C（紙面の濃淡・新聞一面型）を基本に、
  ①トップ記事「本日の最重要」の**1件だけ**案Bの要約主役（全項目に広げると要約の定型語尾が
  原題の定型の置き換えになるだけ）②中セクションは案Aの階層 ③低は背景色の索引
  （**人事異動と報酬改定を同じ重さで並べない——このツールの主張の中心**）。
- **[DECISION] トップ記事の選定規準**: その日の「高」のうち**日付が最新**の1件
  （同日付なら巡回順の先頭）。AIに追加で選ばせない（判定回数と揺らぎを増やさない）。
  高0件の日はトップを立てない（「最重要」の看板は高にのみ）。新着0件は従来の1行表示。
  絞り込みでトップが対象外になったら**次点を繰り上げず**トップごと畳む。
- **[DECISION] 不採用（ユーザー確定）**: 点滅（光過敏配慮・意味とずれる）／常時ティッカー／
  「今日は静かな日」等の擬人化表現（公式の記録装置として語りかけない・AI感の排除）。
- **[DECISION] 情報源の識別はロゴを使わず**（著作権）、設置主体の種別記号＋名称で組み分ける
  （［国］実線枠／［独法］破線枠／［県］二重線枠）。
- ★ モック3案（mock/ 配下・Pages公開で実機比較）はP8承認後に撤去済み（2026-08-17）。

## 2026-08-17（P8）

- **[DECISION] デザイン正本は news-layout-sample-v3-pc.html**（ユーザー支給）＋確定変更点2つ:
  題字下の「通算N件」削除／分野の並びは全箇所**保育・児童・障害・高齢**（共通の4色下線も同順）。
- **[DECISION] グラフ「この7日」の再定義**: 情報量のグラフでなく**「対応が要る日の地図」**。
  高だけ朱・中低は無彩色の薄階調・高の日はバー下に朱点・タップでその日の紙面へ
  （グラフを目次にする）。**P9で報道が入ってもバーには含めない**
  （主語は「行政の確定情報のうち対応が要るもの」）。
- **[DECISION] 検索は「直近3か月→3か月ずつ遡る」方式**。対象=タイトル・要約・源・分野。
  NFKC正規化+空白AND部分一致。検索した範囲を結果に必ず明記（サイレント縮小をしない）。
- **[DECISION] AIの働きかけ**: (a)対応の目安=**高のみ**・判定本体と別の第2リクエストで生成
  （本体の件数不一致リスクを増やさない）・失敗時はaction無しで続行・
  距離感規則（「一般に〜が必要になります」・命令形/あなた禁止）・表示はタップ展開。
  (b)関連参照=**AI照合でなく系列キーの機械照合**（回数・日付を除いたタイトル・6文字未満不使用・
  当月+前月から最大3件）。捏造リスクゼロ・API消費ゼロ。無料枠試算: 計2リクエスト/日≒1%未満。
- **[DECISION] 号数**: 創刊2026-08-16=第1号。号数=経過日数+1（下限1）。新着0件でも号は進む。
- **[DECISION] スマホ幅(900px未満)のみ上部圧縮**: 題字縮小・副題非表示・欄外1行・
  月ナビ廃止（日付見出し行の矢印に）・**重要度チップ廃止**（紙面が高→中→低順で低は索引済み）。
  題字のスクロール固定は**不採用**（常時固定バーは占有削減の目的に逆行）。PC幅は無変更。
- ★ 教訓: 長時間セッション中に定期実行が走ると push が拒否される（fetch first）。
  `git pull --rebase` で自動コミットを取り込んでから push すればよい。

## 2026-08-18（P9）

- **[DECISION] 報道は行政と別区分で扱う**: diff（newPress）・report（press）・履歴（days[].press）
  すべて別配列。counts・index.json（グラフ材料）・件名の件数・分野重み付けに**入らない構造**で分離
  （既存の行政パイプラインの件数検証の前提を崩さない）。
- **[DECISION] 報道にはAI要約・重要度判定をしない**（見出しと出典の表示のみ・翻案リスク回避と
  「報道を加工しない」姿勢の表明）。分野タグのみ第3の小リクエストで付与し、
  **出力は index と fields だけ**（見出しの複製も出力させない）。失敗時はタグ空で続行。
- **[DECISION] 保存するのは見出し・リンク・日付・カテゴリのみ**。RSSの説明文・本文は読み捨てる。
- **[DECISION] 福祉新聞は公開前に編集部へ一報**（「見出しの無断転載禁止」の明記があるため）。
  回答まで sources.md 状態=保留（照会中）・巡回対象外。Joint（禁止文言なし）は先行運用。
  復帰手順: 状態を「巡回中」に戻すだけ（パーサ・stateベースライン温存済み）。
- ★ 調査メモ: 福祉新聞=robots全許可・RSS約1本/日・見出し転載禁止の明記あり。
  Joint=robots許可・RSS約1〜2本/日・禁止文言確認できず。Google News RSSは不採用
  （リンクが中間URLで原本直リンク原則に反する・ノイズ過多を実証確認）。
  ニュース見出しの著作物性は判例上原則否定（読売事件）だが、デッドコピー的利用は不法行為になりうる。

## 2026-08-18（P10）

- **[DECISION] スマホの固定ヘッダーは「スペーサ｜題字（可変幅・nowrap）｜ボタン群」のフレックス1行**
  （重なりを構造的に排除）。**行高は固定**（欄外34px＋題字行52px＝計86px）で、
  スクロール時は題字の文字サイズだけを縮小（21→15px）——紙面が跳ねない。
- **[DECISION] メニューは右差し込みドロワー**（紙面を押し出さない・背景タップで閉じる）。
  中身の順序: この7日→今週の記録→記号の見かた→このサイトについて（さらに折りたたみ）。
  **分野チップはメニューに入れず紙面（日付見出し直下）に残す**
  （見え方を変える操作は目の前で。メニュー越しの3手では使われない）。
- **[DECISION] PCサイド欄はスマホメニューと同順**: 検索→分野→この7日→今週の記録→記号の見かた→
  このサイトについて（折りたたみ・既定閉。本文下からは削除）。
- ★ 実装の型: サイド欄の中身・チップ・aboutは**DOMノードを幅に応じて移設**する
  （描画ロジックとイベントを複製しない）。aboutは常に折りたたみ要素内に置き、
  折りたたみごと移動。relocate は冪等にし、matchMedia change に加えて **resize でも呼ぶ**
  （change が飛ばない環境の保険）。
- ★ 教訓: プレビューペインのレンダラーが描画停止すると **CSS transition が進まず
  「スタイルが効かない」ように見える**（スクリーンショットも白くなる）。切り分けは
  `transition:none` にして最終状態を機械確認する（鉄則4: ツール制約と実装不合格の区別）。
- ★ 教訓（P10修正で2回外した検証の穴）:
  ①**状態を持つUIは全状態で検証する**（スクロール前/後・開閉・選択中など）。片方だけの確認が
  見落としを生んだ実例。②**テキストのはみ出し検証に scrollWidth を使わない**——ブロック要素の
  scrollWidth は箱幅未満にならないため「余白ゼロ＝ぴったり」に見えても実際は収まっている/
  はみ出しているの区別がつかない。**Range.getBoundingClientRect() でテキスト実幅**を測る。

## 2026-08-18（P11）

- **[DECISION] 本文への追加巡回を解禁（P2の見送りを更新）**: 対象は**当日の高・中のみ**
  （締切を持つ層をカバー・研修申込は中に多い実証あり）。間隔1.5秒・UA明示・本文は抽出後破棄。
- **[DECISION] 締切抽出は項目ごとの個別小リクエスト**（判定本体に不介入=P6教訓の維持。
  バッチだと本文×Nでプロンプト肥大・件数不一致リスクが再来するため）。
- **[DECISION] 幻覚ガード三重検証を必須とする**: ①根拠文字列(quote)が取得本文に完全一致で
  実在 ②quoteから自前パーサ（令和N年=N+2018・全角・年なし対応）で日付を機械再抽出しAIの
  dateと一致 ③今日〜2年先の範囲。3つ全て通った期限のみ採用。単体テスト7/7。
- **[DECISION] 共有リンクは `?d=YYYY-MM-DD&i=hash8` のクエリ方式・全項目に付与**（低・報道含む。
  「1件を人に送る」用途は重要度と無関係）。コピー導線は鎖アイコンのみ・説明文は常時出さない。
- **[DECISION] 締切ウォッチは付加機能として失敗隔離**: 1件の404等は続行、スクリプト全体の失敗も
  exit 0（メール・履歴の本体を止めない）。期限切れは表示からも**保存からも**自動削除（最小化方針）。
- ★ 実測: 対象4件で32秒（Actions全体は対象がある朝で90秒前後の見込み・対象0件日は46秒）。
  初回抽出2件はどちらも一覧タイトルに無い本文内の期限（研修申込9/18・傍聴申込8/20）で承認済み。
- ★ 教訓: レンダラー凍結環境では requestAnimationFrame も止まる。**初期化系の1回きりの
  スクロール等は rAF でなく setTimeout** にしておくと検証環境でも実機でも安全。

---

# ロードマップ

| フェーズ | 内容 | 完了条件 | 状態 |
|---|---|---|---|
| **P0** | 足場作り | CLAUDE.md・台帳・`docs/sources.md` が push 済み | ✅ 完了（2026-08-16） |
| **P1** | 巡回＋差分検知。**まず1源だけ**で URL確定 → 取得 → ハッシュ＋見出しの差分検知を貫通させる | 手動実行で差分が検知できる | ✅ 完了（2026-08-16） |
| **P2** | Gemini による要約＋重要度判定（**冒頭に手順0＝APIキー**） | 差分から日本語要約と、法人への関連度判定が出る | ✅ 完了（2026-08-16） |
| **P3** | Gmail 送信＋定期実行（**冒頭に手順0＝アプリパスワード**） | 毎朝7時に自動でメールが届く＝**実運用開始** | ✅ 完了（2026-08-16） |
| **P4** | ダッシュボード（GitHub Pages で履歴を公開。`report-latest.json` を共通の材料とする） | 履歴がブラウザで見られる | ✅ 完了（2026-08-16・公開URLでユーザー承認） |
| **P5** | 監視源の拡張。残り4源のURL確定 → 以降は**台帳に1行追加するだけ**で増設できる状態にする | 5源すべてが巡回中。1行追加で増設できる | ✅ 完了（2026-08-16・P4より先に実施） |
| **P6** | 分野タグ（保育・障害・高齢・児童・共通）。**P4の公開より先に実施**［DECISION・順序変更 2026-08-16］: 分野は「設定」でなく「**データに付ける属性**」。メール側=リポジトリ内JSONで重み付け／ダッシュボード側=動的切替 | 判定出力に分野タグが付き、メール側の重み付けが動く | ✅ 完了（2026-08-16） |
| （再掲）P4公開 | ダッシュボードに分野切替UIを追加 → Pages公開 → 区切り④検証 | 公開URLでユーザー承認 | ✅ 完了（2026-08-16） |
| **P7** | UIの磨き込み（モック先行）。重要度の差を文字サイズ・余白・組みの濃淡としてレイアウトに定着させる。トップ記事の選定規準=高のうち日付最新の1件（高0の日は立てない・絞り込みで繰り上げない） | 統合案のユーザー承認と本体実装 | ✅ 完了（2026-08-16。mock/はP8承認後に撤去済み） |
| **P8** | 紙面改訂（正本=news-layout-sample-v3-pc.html＋変更点）＋検索＋グラフ再定義＋AIの働きかけ＋スマホ上部圧縮 | PC/スマホ実機承認・Actions全パイプライン成功 | ✅ 完了（2026-08-17） |
| **P9** | 報道セクション（福祉系ニュース）。行政と別区分・要約/判定なし・見出しと出典のみ・グラフ不算入 | 実データでメール/画面確認・回帰確認・Actions成功 | ✅ 完了（2026-08-18・**福祉新聞のみ照会待ちで保留**、Jointで運用中） |
| **P10** | スマホのヘッダー固定＋メニュー化（右差し込みドロワー・検索スライド・分野チップは紙面に残す）＋PCサイド欄の同順再編 | スマホ/PC実機承認 | ✅ 完了（2026-08-18） |
| **P11** | 締切ウォッチ（本文巡回解禁・幻覚ガード三重検証）＋項目ごとの共有リンク | 実データ抽出の妥当性承認・コールドロード両幅検証・Actions成功 | ✅ 完了（2026-08-18） |
| **P12** | 文言修正（「個人開発の」削除）＋Web Share API（共有シート・タイトルとURLのみ） | スマホ実機で共有シート確認 | ✅ 完了（2026-08-18） |
| **P13** | 検索例「研修 申込」＋セクション見出しの階層明示（別系統の字面・太罫・余白。色は増やさない） | 実機承認 | ✅ 完了（2026-08-18） |

### P6 の設計方針の候補（P6の区切り①で検討する。今は実装しない）

- **分野は「絞り込み」ではなく「重要度の重み付け」に使う。**
  選んだ分野外を除外すると、報酬改定・虐待防止など**全分野に効く通知を取りこぼす**ため、
  関係の薄いものは低に落として残す。
- **設定の置き場所**（リポジトリ内のJSON ／ P4のダッシュボードにUIを置く）は、
  **P4 の設計と合わせて決める**。
- **意義**: 判定の前提が可変になれば、高齢分野の法人や**県協会の会員施設にもそのまま配れる
  ツール**になる（台帳冒頭の「昇格前提」と直結する布石）。

---

# 経緯アーカイブ

（まだ無し。10件を超えたら `fukushi-watch-archive-YYYY-MM.md` へ原文のまま移す）
