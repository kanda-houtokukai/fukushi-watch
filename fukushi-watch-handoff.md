# 福祉情報ウォッチ — 引き継ぎ台帳（handoff）

> 引き継ぎの入口はこのファイル1本。新しいチャットは、まずここを読んでから作業を始める。
> 二層運用: ①現在地サマリ（毎回読む）②経緯アーカイブ（必要なときだけ読む）。

---

# 現在地サマリ

## 現在地

- **P0（足場作り）完了。** リポジトリ作成・CLAUDE.md 設置・この台帳の初期化・監視対象台帳
  （`docs/sources.md`）の器の作成まで。**コードは1行も書いていない。**
- 監視対象は初期5源（こども家庭庁／厚労省／WAM NET／福岡県／報酬改定関連）を行だけ用意した状態。
  全行が「URL未確定」。

## 次の一手

- **P1の実装指示書を待つ（停止中）。** 指示書が来るまで実装・調査に着手しない。
- P1で行う想定: 各源のURL調査・確定 → 巡回方法の決定 → `docs/sources.md` の状態更新。

## 生きている注意事項

- ★ **差分スナップショットに監視先ページの全文をコピーして保存しない。**
  将来にわたって、保存するのは**ハッシュ＋見出し等の最小限**にとどめる。
  理由: ①官公庁ページの全文保存は著作権上の配慮が要る ②全文を貯めるとリポジトリが肥大し、
  差分の確認そのものが重くなる ③変更の検知に必要なのは「変わったか」と「どこが変わったか」であり、
  本文の複製ではない。**原本への直リンクを常に残し、最終判断は原本確認に誘導する**（dev-workflow 鉄則5）。
- ★ **P0ではコードを書かない。** 器（フォルダ・台帳・CLAUDE.md）だけを作る。
- ★ `docs/sources.md` の**確定済みの行は承認なしに書き換えない**。URLの確定はP1の作業。
- ★ 報酬改定関連は **報酬改定ナビ**（`~/Documents/houshu-navi/`・kanda-houtokukai/houshu-navi）と
  情報源が重なる可能性がある。P1で突き合わせ、重複監視を避ける。
- ★ 法人PT案件のため、PTの役割は**「可能性の提示」まで**（本運用の判断はPT外＝推進会議）。
  本運用まで抱え込む設計にしない。予算は**AIサブスクリプションのみが基本**で、
  新たなコストが出る選択肢は完全無料の代替を検討してから提示する。

## ブロッカー

- なし（P1指示書待ちの停止中）。

## ファイルの地図

| ファイル | 役割 | 正本か |
|---|---|---|
| `fukushi-watch-handoff.md` | この台帳。引き継ぎの入口 | ✅ 正本 |
| `CLAUDE.md` | Code がセッション開始時に自動で読む案件固有ルール | ✅ 正本 |
| `docs/sources.md` | 監視対象台帳（源の名前／URL／巡回方法／状態） | ✅ 正本 |
| `~/Documents/fukushi-watch/` | ローカルの作業場所 | — |
| `github.com/kanda-houtokukai/fukushi-watch` | 公開リポジトリ | — |
| `~/.claude/skills/dev-workflow/` | 作業規範（全案件共通） | ✅ 正本 |

---

# 決定事項（[DECISION]）

## P0（2026-08-16）

- **[DECISION] 案件名は「福祉情報ウォッチ」。種別は法人PT案件（豊徳会・AI業務効率化PT）。**
  理由: 制度情報の追跡は法人文化「先回りして備える」に直結し、11事業所→連携協議会25法人への
  波及視野に乗る領域のため。
- **[DECISION] ローカルは `~/Documents/fukushi-watch/`、公開リポジトリは
  `kanda-houtokukai/fukushi-watch`（public）。**
  理由: 報酬改定ナビ（同 owner・`~/Documents/houshu-navi/`）と同じ配置規則に揃え、
  raw での直読み（dev-workflow §8）を可能にするため。
- **[DECISION] P0のスコープは足場作りのみ。コードは書かない。**
  理由: 監視対象のURLも巡回方法も未確定な段階で実装に入ると、手戻りが実装ごと発生するため
  （dev-workflow 鉄則2・7-1）。
- **[DECISION] 監視対象は `docs/sources.md` を正本として台帳管理する。初期5源は
  こども家庭庁／厚労省／WAM NET／福岡県／報酬改定関連。P0では行だけ用意し、
  URLの調査・確定はP1で行う。**
  理由: 情報源の増減は運用の中心的な変更点であり、コードに埋め込むと変更のたびに実装が要る。
  台帳を先に正本化しておけば、P1以降は台帳を編集するだけで運用できる。
- **[DECISION] 差分スナップショットは全文コピーを保存せず、ハッシュ＋見出し等の最小限にとどめる
  （将来にわたる方針）。**
  理由: 上記「生きている注意事項」に記載のとおり（著作権への配慮・リポジトリ肥大の回避・
  検知に必要な情報だけを持つ）。
- **[DECISION] 台帳の維持規則は「版/フェーズの記録は直近10件・3.2万字」。超過分は原文のまま
  月別アーカイブへ移す（要約による削減は禁止）。**
  理由: 台帳は毎セッション読まれるため、肥大がそのままコンテキストの浪費になる（dev-workflow 鉄則6）。

> ⚠️ **未反映**: Chat側で確定した決定事項リスト（1〜10）とロードマップの本文が未着。
> 受領し次第、上記に統合し、ロードマップの節（P0〜）をこの台帳に追加する。

---

# ロードマップ

| フェーズ | 内容 | 状態 |
|---|---|---|
| P0 | 足場作り（リポジトリ・CLAUDE.md・台帳・監視対象台帳の器） | ✅ 完了（2026-08-16） |
| P1 | 未定（指示書待ち。想定: 各源のURL調査・確定と巡回方法の決定） | ⏸ 待機中 |

> ⚠️ Chat側で確定したロードマップ本文が未着のため、現時点では P0/P1 のみの暫定表。
> 受領後に正式なフェーズ分割と各フェーズの完了条件で置き換える。

---

# 経緯アーカイブ

（まだ無し。10件を超えたら `fukushi-watch-archive-YYYY-MM.md` へ原文のまま移す）
